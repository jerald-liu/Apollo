line-length = 100
target-version = "py311"

[lint]
# Minimal: catch real bugs, not style nits.
select = ["F", "E9"]
ignore = ["F401", "F541", "F841"]  # unused imports / f-strings / vars OK in research code

[lint.per-file-ignores]
"tests/**" = ["F401", "F811"]
