[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: fast, isolated, no disk or network I/O beyond tmp_path
    integration: exercises multiple modules together or runs scripts
    e2e: full training/generation flow (not run by default)
addopts = -ra --strict-markers
