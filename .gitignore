# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Environments
venv/
.venv/
env/

# Data + artifacts (never commit large files or model state)
data/
logs/
models/
*.pt
*.ckpt
*.npy
*.npz

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Tooling / session local
.claude/

# Build / packaging
build/
dist/

# Apollo runtime artifacts
artifacts/

# Phase 4 — eval loop regenerable data
eval/scores.jsonl
eval/runs.jsonl
eval/render_manifests/*.json
!eval/render_manifests/.gitkeep
data/pairs/*/eval/

# Phase 6 — rendered call.wav is a DERIVED artifact (reproducible from
# call_fm.json + call.mid via apollo.scripts.render_corpus). Authored
# call.mid / call_fm.json / response.mid stay tracked.
data/pairs/**/call.wav
