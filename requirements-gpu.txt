# Pinned for reproducible GPU training builds.
# Install with: pip install -r requirements-gpu.txt
# PyTorch must come first — it sets the CUDA ABI everything else links against.
--index-url https://download.pytorch.org/whl/cu121
torch==2.3.1

--index-url https://pypi.org/simple
numpy==1.26.4
pandas==2.2.2
pretty_midi==0.2.10
mido==1.3.2
librosa==0.10.2
tqdm==4.66.4
PyYAML==6.0.1
wandb==0.17.0
