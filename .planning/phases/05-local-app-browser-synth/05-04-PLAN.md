---
phase: 05-local-app-browser-synth
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - apollo/app/app.py
  - apollo/app/static/editor.js
  - apollo/app/static/app.js
  - apollo/app/templates/generate.html
  - apollo/app/presets/bright_bell.json
  - apollo/app/presets/warm_pad.json
  - apollo/app/presets/vibrato_lead.json
  - tests/test_app_generate.py
autonomous: true
requirements: [APP-04, APP-06, APP-09, APP-13]
user_setup: []

must_haves:
  truths:
    - "The patch editor exposes algorithm selector + per-operator ratio/level/ADSR + an optional collapsible LFO section, all client-validated against spec_constants.js BOUNDS"
    - "Editing any control immediately previews a test note through the browser FM synth"
    - "Saving the editor produces a call_fm.json that apollo.synth.load_manifest accepts verbatim"
    - "A user uploads call.mid + authors a patch, POST /generate subprocesses generate.py, and response.mid is written to the configurable store and auditioned through the call's own patch (D-17)"
    - "At least 3 bundled presets covering different algorithms are loadable as editor starting points"
  artifacts:
    - path: "apollo/app/static/editor.js"
      provides: "Patch editor UI: build controls, validate, serialize call_fm.json, live preview"
      contains: "checkBounds"
      min_lines: 80
    - path: "apollo/app/templates/generate.html"
      provides: "MIDI upload + patch editor + generate + response list"
      contains: "patch-editor"
    - path: "apollo/app/static/app.js"
      provides: "editor mount + preset wiring + /generate multipart handler + /responses refresh"
      contains: "ApolloEditor"
    - path: "apollo/app/app.py"
      provides: "/generate route + /presets + GET /responses"
      contains: "def generate"
    - path: "apollo/app/presets/bright_bell.json"
      provides: "bundled starter preset"
      contains: "operators"
  key_links:
    - from: "apollo/app/static/editor.js"
      to: "spec_constants.js checkBounds/BOUNDS"
      via: "client-side validation before serialize"
      pattern: "checkBounds"
    - from: "apollo/app/static/editor.js"
      to: "ApolloSynth.playSequence"
      via: "live preview of edited patch"
      pattern: "ApolloSynth"
    - from: "apollo/app/app.py::/generate"
      to: "apollo.scripts.generate (subprocess) + RESPONSES_DIR"
      via: "subprocess + copy response.mid to store"
      pattern: "generate"
---

<objective>
Build the showpiece patch editor (D-18/D-19/D-20): algorithm + per-operator + optional LFO controls in lockstep with `spec_constants.js`, live preview through the browser synth, and serialization to a `call_fm.json` the server accepts verbatim. Then the call→response flow: upload call.mid + author a patch, subprocess the shipped generate.py, write response.mid to the configurable store, and audition it through the call's own patch. Plus 3 bundled starter presets.

Purpose: The interactive authoring + generation experience that makes the "you answering yourself" loop tangible in-browser, with no Ableton.
Output: `editor.js`, `generate.html`, `/generate` + `/presets` + `/responses` routes, editor wiring in `app.js`, and 3 preset files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-local-app-browser-synth/05-CONTEXT.md
@.planning/phases/05-local-app-browser-synth/05-RESEARCH.md
@.planning/phases/05-local-app-browser-synth/05-PATTERNS.md
@.planning/phases/05-local-app-browser-synth/05-UI-SPEC.md
@.planning/phases/05-local-app-browser-synth/05-03-SUMMARY.md

<interfaces>
<!-- Shipped + Plan 01-03 interfaces this plan consumes. -->

apollo/scripts/generate.py CLI (subprocessed):
  positional: checkpoint (path to .pt), call_mid (path to call.mid)
  flags: --n (default 1), --temperature (0.8), --top-k (10), --max-tokens (24)
  BEHAVIOR: call.wav is rendered ON-THE-FLY from sibling <pair_dir>/call_fm.json (train/serve parity);
            response written as <pair_dir>/response_NNN.mid (next free index, never overwrites). NO --output-dir flag.
  → After generate, the app must COPY/move the new response_NNN.mid from the pair dir into RESPONSES_DIR (D-12).
  Checkpoint selection (RESEARCH OQ2): use most-recent .pt under models/ (max mtime). Surface path in UI.

call_fm.json schema the editor must emit (load_manifest-valid):
  { "spec_version": "1.1", "algorithm": int(0..2), "gain": float(0..1),
    "operators": [ {"ratio":0.5..12, "level":0..1, "attack":0..2, "decay":0..2, "sustain":0..1, "release":0..2} x3 ],
    "lfo": {"rate":0.05..20, "depth":0..1, "wave":0..2, "target":0..1}  // OMIT key entirely when LFO disabled
  }
  NOTE: when LFO is OFF, the editor MUST NOT include an "lfo" key (absent ⇒ v1.0-identical render). spec_version stays "1.1" but lfo-absent is valid.

From spec_constants.js (Plan 01): SPEC_VERSION, N_OPERATORS=3, ALGORITHMS, BOUNDS, LFO_WAVES, LFO_TARGETS, checkBounds(value, field).
From synth.js (Plan 02): window.ApolloSynth.playSequence(ctx, patch, notes).
From app.js (Plan 02/03): getCtx(), showError().
From app.py (Plan 03): app.config["RESPONSES_DIR"], _allocate_next_nnn(), _known_pairs_set().
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: editor.js — patch editor with validation + live preview + 3 presets</name>
  <read_first>
    - apollo/app/static/spec_constants.js (BOUNDS, ALGORITHMS, LFO_WAVES, LFO_TARGETS, checkBounds — Plan 01)
    - apollo/app/static/synth.js (ApolloSynth.playSequence — Plan 02)
    - apollo/synth/spec.py (OperatorParams/FmParams/LfoParams field names + ranges lines 85-153)
    - apollo/synth/manifest.py (load_manifest field expectations lines 120-193 — what the server will accept)
    - .planning/phases/05-local-app-browser-synth/05-UI-SPEC.md (patch editor layout, LFO collapsible section, Color/Typography)
    - .planning/phases/05-local-app-browser-synth/05-PATTERNS.md (op-panel/lfo-section CSS, editor role)
  </read_first>
  <files>apollo/app/static/editor.js, apollo/app/presets/bright_bell.json, apollo/app/presets/warm_pad.json, apollo/app/presets/vibrato_lead.json</files>
  <action>
Three preset files (each a load_manifest-valid call_fm.json, spec_version "1.1"):
- `bright_bell.json`: algorithm 0 (STACK), gain 0.85, ops e.g. op1{ratio 1.0, level 0.9, a 0.005, d 0.4, s 0.2, r 0.3}, op2{ratio 3.5, level 0.6, ...}, op3{ratio 7.0, level 0.4, ...}; NO lfo key.
- `warm_pad.json`: algorithm 1 (PARALLEL_MODS), gain 0.7, slow attacks (a 0.4, s 0.8, r 0.6), ratios near integer; with lfo {rate 0.3, depth 0.4, wave 0, target 0} (slow tremolo).
- `vibrato_lead.json`: algorithm 2 (CARRIER_PAIR), gain 0.8, with lfo {rate 5.0, depth 0.6, wave 0, target 1} (vibrato).
Each value MUST be within BOUNDS (ratio 0.5-12, level/sustain/gain 0-1, adsr 0-2, lfo_rate 0.05-20, lfo_depth 0-1, wave 0-2, target 0-1).

`apollo/app/static/editor.js` (vanilla, no deps; depends on spec_constants.js + synth.js + app.js helpers being loaded first):
- `buildEditor(container)`: render an algorithm `<select>` populated from `ALGORITHMS` (option value = numeric); 3 `.op-panel` blocks each with number/range inputs for ratio/level/attack/decay/sustain/release labeled with their BOUNDS as min/max/step; a master `gain` input; and a collapsible LFO section `<details class="lfo-section"><summary>LFO (optional)</summary>...</details>` with an enable checkbox plus rate/depth inputs, a wave `<select>` from `LFO_WAVES`, and a target `<select>` from `LFO_TARGETS`. Set each numeric input's `min`/`max`/`step` from `BOUNDS[field]`.
- `readPatch()`: collect values into the call_fm.json object. spec_version = "1.1"; algorithm = Number(select); operators = 3 objects (Number() each field); gain = Number(gain). If the LFO enable checkbox is checked, include `lfo: {rate, depth, wave: Number, target: Number}`; if unchecked, OMIT the lfo key entirely (absent ⇒ v1.0-identical render).
- `validatePatch(patch)`: for each operator field and gain, call `checkBounds(value, field)`; if lfo present, checkBounds on lfo_rate/lfo_depth and assert wave∈{0,1,2}, target∈{0,1}. Return {ok, errors[]}. (Dual validation — client rejects before send; server re-validates via load_manifest, D-13.)
- `previewNote()`: build a short 2-note test sequence (e.g. pitch 60 then 64, each 0.4s) as note objects `[{pitch,velocity:100,start,duration}]`; `window.ApolloSynth.playSequence(getCtx(), readPatch(), notes)`. Call this on any control `input`/`change` event (debounced ~150ms) so editing immediately previews (APP-06).
- `loadPreset(presetObj)`: populate all inputs from a preset object (set LFO checkbox + fields if preset has lfo).
- `applyPresetByName(name)`: `fetch('/presets/' + name).then(r=>r.json()).then(loadPreset)`.
- Expose `window.ApolloEditor = { buildEditor, readPatch, validatePatch, previewNote, loadPreset, applyPresetByName }`.
  </action>
  <verify>
    <automated>python -c "
import json
from apollo.synth.manifest import load_manifest
import tempfile, os
for name in ['bright_bell','warm_pad','vibrato_lead']:
    p='apollo/app/presets/%s.json'%name
    fp=load_manifest(p, p)  # must NOT raise
    print(name,'algorithm',fp.algorithm,'lfo',fp.lfo)
print('OK')"
node -e "const fs=require('fs');const s=fs.readFileSync('apollo/app/static/editor.js','utf8');for(const t of ['checkBounds','readPatch','ApolloSynth','spec_version','lfo']){if(!s.includes(t)){console.error('MISSING',t);process.exit(1);}}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 preset files load via `apollo.synth.manifest.load_manifest` without raising (server accepts the exact JSON the editor mirrors)
    - The 3 presets use 3 DIFFERENT algorithms (0, 1, 2) — `grep -h '"algorithm"' apollo/app/presets/*.json` shows 0, 1, and 2
    - `apollo/app/static/editor.js` contains `checkBounds`, `readPatch`, `ApolloSynth`, `spec_version`, and conditionally includes `lfo`
    - editor.js sets input min/max from `BOUNDS` (contains `BOUNDS[`)
    - editor.js ≥ 80 lines; when LFO disabled, readPatch omits the lfo key (code path present)
  </acceptance_criteria>
  <done>Editor builds spec-locked controls, validates against BOUNDS, previews live via the browser synth, loads 3 valid presets; emitted JSON is load_manifest-valid.</done>
</task>

<task type="auto">
  <name>Task 2: /generate + /presets + /responses routes + generate.html</name>
  <read_first>
    - apollo/scripts/generate.py (CLI surface lines 131-160; response_NNN.mid output lines 48-52, 233-234)
    - apollo/app/app.py (Plan 03: RESPONSES_DIR, _allocate_next_nnn, jsonify patterns)
    - apollo/eval/web/templates/pair.html (audio block / button patterns)
    - .planning/phases/05-local-app-browser-synth/05-RESEARCH.md (OQ2 checkpoint selection, OQ3 response path copy, Pattern 6)
    - .planning/phases/05-local-app-browser-synth/05-UI-SPEC.md (Copywriting: "Generate response", no-checkpoint error, empty responses state)
  </read_first>
  <files>apollo/app/app.py, apollo/app/templates/generate.html</files>
  <action>
`apollo/app/app.py` — add (imports: `import shutil, subprocess`; `glob`/`Path` already present):
- `@app.get("/generate")` → `render_template("generate.html")`.
- `@app.get("/presets/<name>")`: allow-list name to `[a-z_]+` (reject anything else with abort(400)); `path = Path(__file__).parent / "presets" / (name + ".json")`; if not file → abort(404); `return send_file(path, mimetype="application/json")`. Also `@app.get("/presets")` returning `jsonify(sorted(stem for *.json in presets dir))`.
- `_latest_checkpoint() -> Path | None`: glob `models/*.pt`, return max by `st_mtime` or None (RESEARCH OQ2).
- `@app.post("/generate")`: accepts multipart `call_mid` + `call_fm` (the authored patch). Validate manifest first (load_manifest on a temp file → IngestError→400). Allocate a pair dir via `_allocate_next_nnn()`, write call.mid + call_fm.json (this becomes a real pair the user can later keep). `ckpt = _latest_checkpoint()`; if None → `jsonify({"ok": False, "error": "No trained model yet. Add a few pairs and train first, then generate."}), 400` (UI-SPEC copy) and rmtree the dir. Run `subprocess.run(["python","-m","apollo.scripts.generate", str(ckpt), str(pair/"call.mid")], capture_output=True, text=True)`; if returncode != 0 → `jsonify({"ok": False, "error": result.stderr[-500:] or "generation failed"}), 500`. Find the newest `response_*.mid` in the pair dir; `RESPONSES_DIR.mkdir(parents=True, exist_ok=True)`; copy it to `RESPONSES_DIR / f"{pair.name}_{response_name}"` (D-12 configurable store). Return `jsonify({"ok": True, "nnn": pair.name, "response": copied_name, "checkpoint": str(ckpt)})`. argv is a fixed list (no shell, no user string concatenated into the command).
- `@app.get("/responses")`: list files in `RESPONSES_DIR` (if it exists) → `jsonify({"ok": True, "responses": [names]})`.

`apollo/app/templates/generate.html`: `{% extends "base.html" %}`, `{% block title %}Generate{% endblock %}`. Sections:
- MIDI upload: `<input type="file" id="call-mid-input" accept=".mid">`.
- Patch editor mount: `<div id="patch-editor"></div>` plus a preset dropdown `<select id="preset-select"></select>` and a "Preview note" button.
- `<button id="generate-btn" class="primary">Generate response</button>`.
- Responses list region: `<ul id="responses-list"></ul>` with empty-state copy "No responses generated yet" / "Upload or play a call, then hit Generate response to hear what Apollo plays back." (UI-SPEC verbatim).
- `{% block scripts %}<script src="{{ url_for('static', filename='spec_constants.js') }}"></script><script src="{{ url_for('static', filename='synth.js') }}"></script><script src="{{ url_for('static', filename='app.js') }}"></script><script src="{{ url_for('static', filename='editor.js') }}"></script>{% endblock %}`.
  </action>
  <verify>
    <automated>python -c "
from apollo.app.app import create_app
import tempfile
c=create_app(pairs_root=tempfile.mkdtemp()).test_client()
assert c.get('/generate').status_code==200
b=c.get('/generate').get_data(as_text=True)
assert 'patch-editor' in b and 'generate-btn' in b and 'No responses generated yet' in b
pr=c.get('/presets').get_json(); assert 'bright_bell' in pr
assert c.get('/presets/bright_bell').status_code==200
assert c.get('/presets/..%2f..%2fsecrets').status_code in (400,404)
print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `apollo/app/app.py` contains `@app.post("/generate")` building a FIXED argv list for `apollo.scripts.generate` (no `shell=True`, no user string concatenation)
    - /generate returns the no-checkpoint error copy when models/ has no .pt, and removes the allocated dir in that case
    - /generate copies the produced `response_*.mid` into `RESPONSES_DIR` (contains `shutil.copy`/`copy2` into RESPONSES_DIR)
    - `/presets/<name>` allow-lists name (regex/charset check) and rejects traversal (`/presets/..%2f...` → 400 or 404)
    - GET /generate → 200 with `patch-editor`, `generate-btn`, and empty-state copy; GET /presets lists `bright_bell`
  </acceptance_criteria>
  <done>/generate subprocesses generate.py with a fixed argv, handles no-checkpoint, copies response to the configurable store; /presets is traversal-safe; generate.html mounts the editor + responses list.</done>
</task>

<task type="auto">
  <name>Task 3: editor wiring in app.js + generate/preset tests</name>
  <read_first>
    - apollo/app/static/app.js (Plan 02/03 IIFE — extend with editor mount + generate handler)
    - apollo/app/static/editor.js (ApolloEditor API — Task 1)
    - apollo/app/app.py (/generate, /presets, /responses — Task 2)
    - tests/test_app_ingest_train.py (Plan 03 fixture conventions, monkeypatch pattern)
  </read_first>
  <files>apollo/app/static/app.js, tests/test_app_generate.py</files>
  <action>
`apollo/app/static/app.js` — extend the SAME IIFE (guard on element existence). If `#patch-editor` exists: `window.ApolloEditor.buildEditor(document.getElementById('patch-editor'))`; populate `#preset-select` from `fetch('/presets')` and on change call `ApolloEditor.applyPresetByName(value)`; wire a Preview-note button to `ApolloEditor.previewNote()`. Wire `#generate-btn` click: read the uploaded `#call-mid-input` file + `ApolloEditor.readPatch()`; run `ApolloEditor.validatePatch(patch)` client-side and `showError(errors.join())` if invalid; else POST multipart to `/generate` (call_mid file + call_fm as a Blob of JSON.stringify(patch)); on success, append the response to `#responses-list` and audition it: `fetch('/midi/'+nnn+'/'+responseFile...)` — note responses are also stored in the pair dir as response_NNN.mid, so audition via `/midi/<nnn>/response_NNN.mid` through the call's OWN patch (D-17): `ApolloSynth.playSequence(getCtx(), patch, notes)`. Refresh `#responses-list` from `fetch('/responses')` on load.

NOTE: the /midi route (Plan 01) currently allow-lists filenames `call.mid` / `response.mid`. Extend that allow-list in app.py to also accept names matching `response_\d+\.mid` (still no path traversal — regex-anchored, file must exist under the validated pair dir). Make this small edit in app.py if needed for response audition. (This is the only app.py change in this task; keep it minimal.)

`tests/test_app_generate.py`: tmp_path pairs_root. Tests:
- `test_presets_listed_and_valid`: GET /presets includes the 3 names; GET /presets/bright_bell → 200 JSON.
- `test_preset_traversal_rejected`: GET /presets with a traversal-ish name → 400/404.
- `test_generate_no_checkpoint`: ensure no models/*.pt visible (point cwd/models empty via monkeypatch of `_latest_checkpoint` to return None); POST /generate with valid call_mid + call_fm → 400 with the no-checkpoint copy; assert no orphan pair dir remains.
- `test_generate_happy_path_stubbed`: monkeypatch `_latest_checkpoint` to a dummy path AND monkeypatch `subprocess.run` (in app module) to simulate generate.py: write a `response_000.mid` into the pair dir and return returncode 0; POST /generate → 200 `ok True`; assert a file was copied into RESPONSES_DIR. (Avoids loading a real checkpoint / running inference.)
- `test_responses_endpoint`: after a stubbed generate, GET /responses lists the copied file.
  </action>
  <verify>
    <automated>python -m pytest tests/test_app_generate.py -x -q && python -m pytest tests/ -q -k "app_ and not slow"</automated>
  </verify>
  <acceptance_criteria>
    - `apollo/app/static/app.js` contains `ApolloEditor.buildEditor` and POSTs to `/generate` and refreshes `/responses`
    - `tests/test_app_generate.py` contains `test_generate_no_checkpoint` and `test_generate_happy_path_stubbed` and `test_preset_traversal_rejected`
    - The happy-path test stubs `subprocess.run` (no real inference) and asserts a response file lands in RESPONSES_DIR
    - `python -m pytest tests/ -q -k "app_ and not slow"` exits 0 (all Phase 5 app tests pass together)
    - /midi allow-list extended to `response_\d+\.mid` via an anchored regex (no traversal)
  </acceptance_criteria>
  <done>Generate flow wired end-to-end (stub-tested): validate → subprocess generate.py → copy to store → audition through call's own patch; all Phase 5 app tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /generate | uploaded call.mid + authored call_fm.json patch (untrusted) |
| browser → GET /presets/<name> | preset name path segment (untrusted) |
| app → generate.py subprocess | argv built from server-controlled paths |
| browser → /midi/<nnn>/response_NNN.mid | response filename segment (untrusted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Tampering (path traversal) | /presets/<name> | mitigate | `name` allow-listed to `[a-z_]+` (charset/regex), path built under the package `presets/` dir, file-existence checked; `/presets/..%2f...` → 400/404 (asserted in tests). |
| T-05-13 | Injection (manifest) | /generate call_fm | mitigate | Same `load_manifest` validation as /ingest before any write or subprocess; numeric-only, bounds/enum enforced; dual client (checkBounds) + server validation (D-13). |
| T-05-14 | Command/argument injection | /generate subprocess | mitigate | Fixed argv list (`apollo.scripts.generate`, server-resolved checkpoint path, server-allocated pair/call.mid path); no `shell=True`; no user string in argv. |
| T-05-15 | Tampering (path traversal) | /midi response_NNN.mid | mitigate | Filename matched against anchored regex `response_\d+\.mid` (or literal call.mid/response.mid) AND must be an existing file under the `_validate_pair_nnn`-checked pair dir. |
| T-05-16 | Info disclosure | generate.py stderr in error response | accept | Only the last 500 chars of stderr surfaced for debugging a local single-user tool; no secrets in train/generate output. |
</threat_model>

<verification>
- `python -m pytest tests/test_app_generate.py -q` passes; `python -m pytest tests/ -q -k "app_ and not slow"` passes.
- `grep -q 'shell=True' apollo/app/app.py` returns NOTHING.
- 3 presets load via load_manifest with 3 distinct algorithms.
</verification>

<success_criteria>
- Patch editor with algorithm + per-op + optional LFO, spec-locked + client-validated, live preview (APP-04, APP-06).
- Call→response: upload + author patch → generate.py subprocess → response.mid to configurable store, auditioned through call's own patch (APP-09).
- ≥3 bundled presets across different algorithms (APP-13).
</success_criteria>

<output>
After completion, create `.planning/phases/05-local-app-browser-synth/05-04-SUMMARY.md`
</output>
