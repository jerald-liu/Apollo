---
phase: 05-local-app-browser-synth
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - apollo/app/app.py
  - apollo/app/templates/training.html
  - apollo/app/static/app.js
  - tests/test_app_ingest_train.py
autonomous: true
requirements: [APP-03, APP-07, APP-08, APP-12]
user_setup: []

must_haves:
  truths:
    - "A user can upload call.mid + call_fm.json; the server validates via apollo.synth.load_manifest + apollo.ingest.load_notes, writes data/pairs/NNN/, and renders call.wav in-process"
    - "An invalid manifest or MIDI returns the same IngestError reason the CLI pipeline surfaces, with HTTP 400 — the pair is NOT written"
    - "A manual Train button POSTs /train and subprocesses apollo.scripts.train; auto-retrain toggles a server-side debounced run"
    - "The training view shows a live progress bar + loss-over-epochs canvas curve driven by ~1s polling of /status"
    - "The response output directory is configurable and persists for the session (default data/responses/)"
  artifacts:
    - path: "apollo/app/app.py"
      provides: "/ingest, /train, /settings routes + debounced auto-retrain"
      contains: "def ingest"
    - path: "apollo/app/templates/training.html"
      provides: "Train button, auto-retrain toggle, progress bar, loss canvas, storage setting"
      contains: "loss-chart"
    - path: "apollo/app/static/app.js"
      provides: "polling loop + drawLossCurve + ingest/settings fetch handlers"
      contains: "/status"
  key_links:
    - from: "apollo/app/app.py::/ingest"
      to: "apollo.synth.load_manifest + render_call_wav"
      via: "in-process validation + canonical render"
      pattern: "load_manifest|render"
    - from: "apollo/app/app.py::/train"
      to: "apollo/app/jobs.py::TrainingJob.start"
      via: "subprocess apollo.scripts.train"
      pattern: "TRAINING_JOB.*start"
    - from: "apollo/app/static/app.js polling"
      to: "/status"
      via: "setInterval fetch ~1s + drawLossCurve"
      pattern: "setInterval"
---

<objective>
Wire the corpus-building and training backend: a server-side drag-drop ingest that reuses the shipped validators and renders the canonical call.wav in-process, a manual + debounced-auto Train flow that subprocesses the shipped train.py, the live training view (progress bar + loss curve via polling), and a configurable response-storage setting.

Purpose: Close the "build corpus → train locally → watch it learn" loop using only shipped, tested code (D-02/D-05/D-11/D-13), keeping one validation source of truth so app errors match the CLI exactly.
Output: `/ingest`, `/train`, `/settings` routes; `training.html`; polling + ingest handlers in `app.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-local-app-browser-synth/05-CONTEXT.md
@.planning/phases/05-local-app-browser-synth/05-RESEARCH.md
@.planning/phases/05-local-app-browser-synth/05-PATTERNS.md
@.planning/phases/05-local-app-browser-synth/05-02-SUMMARY.md
@.claude/skills/spike-findings-apollo/SKILL.md

<interfaces>
<!-- Shipped signatures this plan calls. Extracted from the codebase. -->

apollo/synth/manifest.py:
  def load_manifest(path: str, pair_path: str) -> FmParams   # raises IngestError(pair_path, reason); takes a FILE PATH
  # validates spec_version {1.0,1.1}, algorithm int, exactly 3 operators, all bounds, optional lfo (lfo requires 1.1)

apollo/synth/render.py:
  def render(params: FmParams, notes, *, pair_path: str) -> np.ndarray   # normalized float32 mono
  def render_call_wav(manifest_path: str, mid_path: str, *, pair_path: str, call_bpm: float = 120.0, notes=None) -> np.ndarray
  # ALWAYS in-process (never subprocess) — RESEARCH Pattern 3. DawDreamer gotchas already handled inside render.py.
  from apollo.synth.spec import SR   # 44100; write WAV with soundfile.write(path, audio, SR)

apollo/ingest/midi.py:
  def load_notes(mid_path: str, pair_path: str, tempo_bpm: float = 120.0) -> List[Note]   # raises IngestError

apollo/scripts/train.py CLI (subprocessed by TrainingJob.start, already built in Plan 01):
  positional: pairs_root ; flags: --epochs (default 300) --output-dir (default models) --log-every (10)
  stdout per log step: "epoch {E}/{N}  train_loss={X:.4f}  held_loss={Y:.4f}"  (regex already in jobs.py)

From Plan 01 app.py: create_app, _known_pairs_set(), _validate_pair_nnn(), app.config["TRAINING_JOB"], ["AUTO_RETRAIN"], ["RESPONSES_DIR"], ["PAIRS_ROOT"].
From Plan 02 app.js: getCtx(), showError(), ApolloSynth (already present — extend the same file).
RESEARCH Code Examples: debounced-auto-retrain (threading.Timer, _DEBOUNCE_SECONDS=3.0), polling loop, drawLossCurve.

Spike landmines (SKILL.md) — already handled inside render.py; do NOT re-implement: integer-index polyphony params, benign `undefined symbol : effect` stderr (suppressed in render.py), no dawdreamer.__version__.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /ingest route (validate → write → render call.wav) + /settings</name>
  <read_first>
    - apollo/synth/manifest.py (load_manifest signature + IngestError contract lines 120-193)
    - apollo/synth/render.py (render + render_call_wav lines 100-222; SR import)
    - apollo/ingest/midi.py (load_notes lines 44-82)
    - apollo/eval/web/app.py (POST handler + jsonify({"ok":...}) pattern lines 162-188)
    - .planning/phases/05-local-app-browser-synth/05-RESEARCH.md (Pattern 6 ingest pipeline; Pitfall 5 call.wav atomic render; Pattern 3 in-process)
    - .planning/phases/05-local-app-browser-synth/05-PATTERNS.md (IngestError→400, NNN allocation, in-process render)
    - .claude/skills/spike-findings-apollo/SKILL.md (DawDreamer gotchas — handled in render.py, do not re-implement)
  </read_first>
  <files>apollo/app/app.py</files>
  <action>
Add to the `apollo/app/app.py` factory (imports: `import json, tempfile, threading`; `import soundfile as sf`; `from apollo.synth.manifest import load_manifest`; `from apollo.synth.render import render`; `from apollo.synth.spec import SR`; `from apollo.ingest.midi import load_notes`; `from apollo.ingest.errors import IngestError`; `from flask import request`).

Module-level (inside create_app closure scope) `_alloc_lock = threading.Lock()` and `_debounce = {"timer": None}`.

`_allocate_next_nnn() -> Path`: under `_alloc_lock`, find max existing 3-digit dir name under PAIRS_ROOT (default -1), `nnn = f"{max+1:03d}"`, `(PAIRS_ROOT/nnn).mkdir(parents=True)`, return it. Holding the lock around find-max+mkdir prevents the concurrent-upload race (RESEARCH OQ4).

`@app.post("/ingest")`:
- `call_mid = request.files.get("call_mid")`; `call_fm = request.files.get("call_fm")`; `response_mid = request.files.get("response_mid")` (optional).
- If `call_mid is None or call_fm is None`: `return jsonify({"ok": False, "error": "call.mid and call_fm.json are both required"}), 400`.
- Read bytes: `fm_bytes = call_fm.read()`, `mid_bytes = call_mid.read()`.
- Validate manifest BEFORE allocating a dir: write `fm_bytes` to a `NamedTemporaryFile(suffix='.json', delete=False)`, then `try: params = load_manifest(tmp_name, "(upload)") except IngestError as e: return jsonify({"ok": False, "error": e.reason}), 400` (finally: unlink tmp). Use pair_path label `"(upload)"` so the error reads cleanly pre-allocation.
- Allocate the pair dir: `pair = _allocate_next_nnn()`.
- Write `(pair/"call_fm.json").write_bytes(fm_bytes)` and `(pair/"call.mid").write_bytes(mid_bytes)`.
- Validate + parse MIDI: `try: notes = load_notes(str(pair/"call.mid"), str(pair)) except IngestError as e: shutil.rmtree(pair); return jsonify({"ok": False, "error": e.reason}), 400` (clean up the partial dir on failure so it is never half-written — Pitfall 5).
- Render canonical call.wav IN-PROCESS (D-11/D-14, never subprocess): `audio = render(params, notes, pair_path=str(pair)); sf.write(str(pair/"call.wav"), audio, SR)`. Wrap in try/except IngestError → rmtree + 400.
- If `response_mid`: `(pair/"response.mid").write_bytes(response_mid.read())`.
- Auto-retrain debounce: if `app.config["AUTO_RETRAIN"]`, cancel `_debounce["timer"]` if present, create `threading.Timer(3.0, lambda: app.config["TRAINING_JOB"].start(str(app.config["PAIRS_ROOT"]), 300, "models"))`, set daemon, start, store in `_debounce["timer"]`. (Bulk drag-in of N pairs → ONE run, D-06.)
- `return jsonify({"ok": True, "nnn": pair.name})`.
(Add `import shutil` at top.)

`@app.post("/settings")`: `data = request.get_json(silent=True) or {}`; if `"responses_dir" in data`: `app.config["RESPONSES_DIR"] = Path(data["responses_dir"]).resolve()`; if `"auto_retrain" in data`: `app.config["AUTO_RETRAIN"] = bool(data["auto_retrain"])`; `return jsonify({"ok": True, "responses_dir": str(app.config["RESPONSES_DIR"]), "auto_retrain": app.config["AUTO_RETRAIN"]})`.

`@app.get("/settings")` → `jsonify({"ok": True, "responses_dir": str(app.config["RESPONSES_DIR"]), "auto_retrain": app.config["AUTO_RETRAIN"]})`.
  </action>
  <verify>
    <automated>python -c "
import io, json
from apollo.app.app import create_app
import tempfile
c = create_app(pairs_root=tempfile.mkdtemp()).test_client()
# bad manifest -> 400, no dir written
bad = c.post('/ingest', data={'call_mid': (io.BytesIO(b''),'call.mid'), 'call_fm': (io.BytesIO(b'{not json'),'call_fm.json')}, content_type='multipart/form-data')
assert bad.status_code==400 and bad.get_json()['ok'] is False, bad.get_json()
s = c.get('/settings').get_json(); assert s['ok'] and 'responses_dir' in s
print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `apollo/app/app.py` contains `@app.post("/ingest")` and calls `load_manifest(` and `render(` (in-process) and `sf.write(`
    - `apollo/app/app.py` does NOT subprocess render (no `Popen` near render; `grep -A3 'render(' apollo/app/app.py` shows no subprocess) — in-process per Pattern 3
    - On IngestError the route returns `jsonify({"ok": False, "error": e.reason}), 400` AND removes any partially-written pair dir (`shutil.rmtree`)
    - Bad-manifest POST /ingest → 400 with `ok: False`; GET /settings → 200 with `responses_dir`
    - auto-retrain uses a `threading.Timer` with 3.0s debounce (one run per bulk drop)
  </acceptance_criteria>
  <done>/ingest validates with shipped validators, renders call.wav in-process, writes a clean pair dir or none; /settings persists responses_dir + auto_retrain.</done>
</task>

<task type="auto">
  <name>Task 2: /train route + training.html + polling/loss-curve in app.js</name>
  <read_first>
    - apollo/app/jobs.py (TrainingJob.start/snapshot — Plan 01)
    - apollo/scripts/train.py (CLI args + stdout format line 174)
    - apollo/eval/web/templates/pair.html (button/disabled + hidden/reveal patterns)
    - .planning/phases/05-local-app-browser-synth/05-RESEARCH.md (Pattern 7 loss curve; Code Examples polling loop)
    - .planning/phases/05-local-app-browser-synth/05-PATTERNS.md (training.html, app.js polling + drawLossCurve excerpts)
    - .planning/phases/05-local-app-browser-synth/05-UI-SPEC.md (Copywriting: "Training locally… epoch {e}/{total}", "Training complete — ready to generate")
  </read_first>
  <files>apollo/app/app.py, apollo/app/templates/training.html, apollo/app/static/app.js</files>
  <action>
`apollo/app/app.py` — add:
- `@app.get("/training")` → `render_template("training.html", responses_dir=str(app.config["RESPONSES_DIR"]), auto_retrain=app.config["AUTO_RETRAIN"])`.
- `@app.post("/train")`: `started = app.config["TRAINING_JOB"].start(str(app.config["PAIRS_ROOT"]), 300, "models")`; if not started → `jsonify({"ok": False, "error": "Training already running"}), 409`; else `jsonify({"ok": True})`. (epochs=300 matches train.py default; D-05 full retrain.)

`apollo/app/templates/training.html`: `{% extends "base.html" %}`, `{% block title %}Training{% endblock %}`. Contains:
- `<button id="train-btn" class="primary">Train model</button>`
- auto-retrain toggle: `<label><input type="checkbox" id="auto-retrain" {% if auto_retrain %}checked{% endif %}> Re-train automatically after each new pair</label>` (UI-SPEC copy)
- progress region (initially hidden): `<div id="training-progress" class="hidden"><p id="train-status" class="mono"></p><div class="progress-bar"><div class="progress-bar-fill" id="progress-fill" style="width:0%"></div></div><div class="loss-chart-wrap"><canvas id="loss-chart" width="600" height="200"></canvas></div></div>`
- storage setting: `<label>Save generated responses to: <input type="text" id="responses-dir" value="{{ responses_dir }}"></label><button id="save-settings" class="secondary">Save</button>`
- `{% block scripts %}<script src="{{ url_for('static', filename='spec_constants.js') }}"></script><script src="{{ url_for('static', filename='app.js') }}"></script>{% endblock %}`

`apollo/app/static/app.js` — extend the SAME IIFE (guard with element-existence checks). Add:
- `drawLossCurve(canvas, history)` — RESEARCH Pattern 7 verbatim: clear; if `history.length<2` return; `maxLoss=Math.max(...history.map(d=>d.train_loss||0))`; toX/toY scalers; stroke train_loss solid `#6D28D9`; stroke held_loss dashed `#15803D` (filter `d.held_loss!=null`).
- `let pollInterval=null; function startPolling(){ if(pollInterval) return; pollInterval=setInterval(async()=>{ const d=await fetch('/status').then(r=>r.json()); updateTrainingUI(d); if(d.status==='complete'||d.status==='error'){clearInterval(pollInterval); pollInterval=null;} }, 1000); }` (~1s, D-07).
- `updateTrainingUI(d)`: reveal `#training-progress` (remove `hidden`); set `#train-status` text to `Training locally… epoch ${d.epoch}/${d.total_epochs}` while running, `Training complete — ready to generate` on complete, error copy on error (UI-SPEC); set `#progress-fill` width `${100*d.epoch/(d.total_epochs||1)}%`; `drawLossCurve(document.getElementById('loss-chart'), d.loss_history||[])`.
- Bind `#train-btn` click → `fetch('/train',{method:'POST'})` then `startPolling()` (handle 409 via showError "Training already running").
- Bind `#auto-retrain` change → `fetch('/settings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({auto_retrain: checkbox.checked})})`.
- Bind `#save-settings` click → POST /settings with `{responses_dir: document.getElementById('responses-dir').value}`; show a confirmation via showError-style message.
- On load, if `#train-btn` exists, immediately `fetch('/status')` once and if status==='running' call `startPolling()` (resume a run in progress).
  </action>
  <verify>
    <automated>python -c "
from apollo.app.app import create_app
import tempfile
c=create_app(pairs_root=tempfile.mkdtemp()).test_client()
assert c.get('/training').status_code==200
b=c.get('/training').get_data(as_text=True)
assert 'train-btn' in b and 'loss-chart' in b and 'auto-retrain' in b
print('OK')"
node -e "const fs=require('fs');const s=fs.readFileSync('apollo/app/static/app.js','utf8');for(const t of ['/status','setInterval','drawLossCurve','/train','/settings']){if(!s.includes(t)){console.error('MISSING',t);process.exit(1);}}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `apollo/app/app.py` contains `@app.post("/train")` calling `TRAINING_JOB` `.start(` with `300` epochs and returns 409 when already running
    - `apollo/app/templates/training.html` contains `train-btn`, `loss-chart`, `auto-retrain`, `progress-bar-fill`, and `responses-dir`
    - `apollo/app/static/app.js` contains `/status`, `setInterval`, `drawLossCurve`, `/train`, `/settings`
    - drawLossCurve strokes both `#6D28D9` (train) and `#15803D` (held)
    - GET /training → 200 with the required element ids
  </acceptance_criteria>
  <done>Train button subprocesses train.py; training view streams epoch/loss via 1s polling with a live canvas loss curve; auto-retrain + storage settings persist.</done>
</task>

<task type="auto">
  <name>Task 3: Ingest + training route tests</name>
  <read_first>
    - apollo/app/app.py (/ingest, /train, /settings routes)
    - tests/test_app_synth.py (Plan 02 test conventions, pretty_midi fixture)
    - apollo/synth/manifest.py (valid manifest shape for the fixture)
  </read_first>
  <files>tests/test_app_ingest_train.py</files>
  <action>
Create `tests/test_app_ingest_train.py` with a `tmp_path` pairs_root and a helper that builds a valid `call.mid` (pretty_midi, one note pitch 60 over 0.5s) bytes and a valid `call_fm.json` bytes (algorithm 0, 3 ops {ratio 1.0, level 0.5, attack 0.01, decay 0.1, sustain 0.7, release 0.1}, gain 0.8, spec_version "1.0"). Tests:
- `test_ingest_valid_pair_renders_wav`: POST /ingest multipart with call_mid + call_fm → 200, `ok True`, returns nnn "000"; assert `data/pairs/000/` (under tmp) now contains call.mid, call_fm.json, AND call.wav (rendered in-process). [This exercises the real DawDreamer render path; allow it to be slow.]
- `test_ingest_bad_manifest_no_dir`: POST with malformed call_fm bytes → 400 `ok False`; assert NO new pair dir was created under PAIRS_ROOT (clean failure, Pitfall 5).
- `test_ingest_bad_midi_cleans_up`: POST valid manifest + empty/invalid call.mid → 400; assert the partially-allocated dir was removed (no orphaned pair).
- `test_train_starts_and_status`: monkeypatch `TrainingJob.start` to a stub returning True and setting status "running" (do NOT actually spawn train.py); POST /train → 200; GET /status reflects the stubbed state. (Avoids a real multi-second training subprocess in the test.)
- `test_settings_roundtrip`: POST /settings {responses_dir: "/tmp/myresp", auto_retrain: true} → 200; GET /settings echoes them back.
  </action>
  <verify>
    <automated>python -m pytest tests/test_app_ingest_train.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_app_ingest_train.py` contains `test_ingest_valid_pair_renders_wav`, `test_ingest_bad_manifest_no_dir`, `test_settings_roundtrip`
    - `python -m pytest tests/test_app_ingest_train.py -q` exits 0 with ≥5 tests passing
    - `test_ingest_bad_manifest_no_dir` asserts the pairs_root has zero pair dirs after a rejected upload
    - The train test does NOT spawn a real subprocess (TrainingJob.start is stubbed/monkeypatched)
  </acceptance_criteria>
  <done>Ingest happy-path renders call.wav, failures leave no orphaned dirs, /train + /settings behavior proven without spawning real training.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → POST /ingest | uploaded call.mid / call_fm.json bytes are untrusted (drag-drop) |
| browser → POST /train, /settings | trigger subprocess + filesystem path config |
| app → train.py subprocess | argv built from server-controlled constants only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering (unsafe file write from drag-drop) | /ingest dir allocation | mitigate | Pair dir is server-allocated via `_allocate_next_nnn` (3-digit, lock-guarded) — the client never supplies the path. Uploaded bytes are written only under that allocated dir. No client-controlled filename reaches the filesystem path. |
| T-05-08 | Injection (malformed manifest/MIDI) | /ingest validation | mitigate | Validate via shipped `load_manifest` (numeric-only, bounds/enum, rejects bools/NaN/Inf) + `load_notes` BEFORE/at write; on IngestError remove the partial dir and return the reason. dsp_string interpolates numeric values only (no string field) — spec.py SECURITY note. |
| T-05-09 | Command/argument injection | /train subprocess | mitigate | `TrainingJob.start` builds argv from a fixed list (`apollo.scripts.train`, server-set pairs_root, integer epochs via `str(int)`, constant output-dir). No user string is concatenated into the command; no `shell=True`. |
| T-05-10 | Tampering (responses_dir path) | /settings responses_dir | accept | Local single-user app; user intentionally chooses their own output dir (D-12). `Path(...).resolve()` normalizes it; writing under a user-chosen local dir is the feature, not a threat. |
| T-05-11 | DoS (render blocks request) | /ingest in-process render | accept | Render is ~0.1–0.2s; `threaded=True` keeps /status polling responsive (RESEARCH Pitfall 1). |
</threat_model>

<verification>
- `python -m pytest tests/test_app_ingest_train.py -q` passes.
- `grep -q 'shell=True' apollo/app/` returns NOTHING; argv is a fixed list in jobs.py.
- /ingest rejects bad manifest with 400 and leaves no orphan dir.
</verification>

<success_criteria>
- Drag-drop ingest writes valid data/pairs/NNN/ with rendered call.wav, reusing shipped validators (APP-03).
- Manual + debounced-auto train via subprocess (APP-07); live progress bar + loss curve via polling (APP-08).
- Configurable response storage persists (APP-12).
</success_criteria>

<output>
After completion, create `.planning/phases/05-local-app-browser-synth/05-03-SUMMARY.md`
</output>
