---
phase: 06-synth-independent-corpus-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - apollo/synth/__init__.py
  - apollo/synth/spec.py
  - apollo/synth/manifest.py
autonomous: true
requirements: [DATA-06]
must_haves:
  truths:
    - "dawdreamer==0.8.3 imports in the SAME project venv as torch on arm64/Py3.11 (no separate env)"
    - "The FM parameter schema, 3 fixed algorithms, and envelope semantics live in one versioned module (single source of truth)"
    - "dsp_string() builds a Faust patch from numeric params only — no manifest string ever reaches the DSP source"
    - "A malformed / out-of-range / wrong-op-count / bad-spec_version manifest raises IngestError naming the pair"
  artifacts:
    - path: "apollo/synth/spec.py"
      provides: "SPEC_VERSION, Algorithm(IntEnum, 3 members), FmParams frozen dataclass, dsp_string(), engine constants SR/BLOCK/NUM_VOICES/TARGET_PEAK"
      contains: "SPEC_VERSION"
      min_lines: 80
    - path: "apollo/synth/manifest.py"
      provides: "load_manifest(path, pair_path) -> FmParams with version + shape + range validation, fail-loud via IngestError"
      contains: "def load_manifest"
      min_lines: 40
    - path: "apollo/synth/__init__.py"
      provides: "package public surface (spec + manifest symbols)"
      contains: "SPEC_VERSION"
    - path: "pyproject.toml"
      provides: "dawdreamer==0.8.3 dependency"
      contains: "dawdreamer==0.8.3"
  key_links:
    - from: "apollo/synth/manifest.py"
      to: "apollo/synth/spec.py"
      via: "imports SPEC_VERSION, FmParams, Algorithm for validation"
      pattern: "from apollo.synth.spec import|from .spec import"
    - from: "apollo/synth/manifest.py"
      to: "apollo/ingest/errors.py"
      via: "raises IngestError(pair_path, reason) on every validation failure"
      pattern: "IngestError"
---

<objective>
Establish the synth-independent rendering foundation: add the dawdreamer dependency (and verify it coexists with torch in ONE project venv — RESEARCH Open Q A4 / Assumption A4), then create the FM **single source of truth** (`spec.py`) and its **manifest validator** (`manifest.py`).

`spec.py` owns the FM parameter schema, the fixed 3-operator algorithm set, envelope semantics, the determinism-critical engine constants, and the ONLY place a Faust DSP string is constructed. `manifest.py` is the input-validation boundary: it loads a per-pair `call_fm.json` and fails loud (`IngestError`) on bad version, wrong operator count, or out-of-range params.

Purpose: Phase 5's browser synth will mirror this exact spec; centralizing schema + topology + envelope math is what guarantees train/serve parity. This is the contract layer every later plan consumes.
Output: `pyproject.toml` (dawdreamer pinned), `apollo/synth/{__init__,spec,manifest}.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase design + locked decisions + validated patterns (READ before implementing)
@.planning/phases/06-synth-independent-corpus-rendering/06-CONTEXT.md
@.planning/phases/06-synth-independent-corpus-rendering/06-RESEARCH.md
@.planning/phases/06-synth-independent-corpus-rendering/06-PATTERNS.md
@.claude/skills/spike-findings-apollo/references/synth-independent-rendering.md

<interfaces>
<!-- Contracts the executor must satisfy / reuse. Use directly — no exploration needed. -->

IngestError (apollo/ingest/errors.py) — REUSE, do not redefine:
```python
class IngestError(Exception):
    def __init__(self, pair_path: str, reason: str):
        # formats as f"[{pair_path}] {reason}"
```

Analog frozen-dataclass-as-source-of-truth (apollo/tokenizer/vocab.py): frozen dataclass +
"do not mutate without bumping version" docstring. Mirror this exact shape for FmParams + SPEC_VERSION.

Analog class-level numeric config constants (apollo/ingest/audio.py MelExtractor lines 35-45):
TARGET_SR=22050, N_FFT=2048, etc., grouped with decision-citing comments. Mirror for engine consts.

MelExtractor security caps to mirror (apollo/ingest/audio.py lines 43-45):
MAX_WAV_BYTES = 10*1024*1024 ; MAX_WAV_SECONDS = 30.0

Proven 2-op Faust DSP skeleton (spike reference lines 14-38) — extend to 3-op:
```faust
import("stdfaust.lib");
freq  = hslider("freq",  440, 20, 20000, 0.01);   // MIDI-owned, NOT settable
gain  = hslider("gain",  0.5,  0,     1, 0.01);    // MIDI-owned, NOT settable
gate  = button("gate");                            // MIDI-owned, NOT settable
ratio = hslider("ratio", 2.0, 0.5,   12, 0.01);    // settable per-op
index = hslider("index", 2.0, 0.0,   12, 0.01);    // settable per-op
env   = en.adsr(0.005, 0.12, 0.7, 0.2, gate);
mod   = os.osc(freq * ratio) * index * freq;
process = os.osc(freq + mod) * env * gain <: _,_;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dawdreamer dependency + verify single-venv install health (Assumption A4)</name>
  <read_first>
    - pyproject.toml (the [project].dependencies block, lines 10-16)
    - .planning/phases/06-synth-independent-corpus-rendering/06-RESEARCH.md (§"Environment Availability", §"Standard Stack" — pin exactly; Open Q A4)
    - .claude/skills/spike-findings-apollo/references/synth-independent-rendering.md (§"Constraints" — spike used isolated venv; this phase requires coexistence with torch)
  </read_first>
  <action>
    Add `"dawdreamer==0.8.3",` to the `[project].dependencies` array in pyproject.toml (pin EXACTLY with `==` per RESEARCH: heavy native arm64 wheel; an accidental bump could change DSP/render behavior and break determinism reproducibility of an already-authored corpus). Then install into the EXISTING project venv (the same venv that has torch) — do NOT create a separate environment. Use the project venv interpreter (the repo's existing `.venv/bin/python` per the CORPUS-CONVENTIONS convention; if a different venv path is in use, match it). Run the install via uv or pip into that venv.

    After install, verify coexistence (this is the A4 risk gate — both torch and dawdreamer are large arm64/Py3.11 native wheels and were never validated in one combined venv): import BOTH `dawdreamer` and `torch` in a single Python process from the project venv and print success. Do NOT reference `dawdreamer.__version__` (it does not exist — spike landmine); confirm presence via `import dawdreamer` succeeding and `hasattr(dawdreamer, "RenderEngine")`.

    If the combined import fails (ABI / wheel conflict on arm64/Py3.11), STOP and report the exact error in the SUMMARY — do not proceed to build features on a broken foundation.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import torch, dawdreamer; assert hasattr(dawdreamer, 'RenderEngine'); print('coexist OK', torch.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'dawdreamer==0.8.3' pyproject.toml` succeeds (exact pin present).
    - `.venv/bin/python -c "import torch, dawdreamer; print('ok')"` exits 0 and prints `ok` (both wheels coexist in one venv).
    - `.venv/bin/python -c "import dawdreamer; assert hasattr(dawdreamer,'RenderEngine')"` exits 0.
    - No use of `dawdreamer.__version__` anywhere (landmine): `! grep -rn 'dawdreamer.__version__' apollo/`.
  </acceptance_criteria>
  <done>dawdreamer==0.8.3 pinned in pyproject.toml and importable alongside torch in the project venv; A4 coexistence confirmed or failure reported.</done>
</task>

<task type="auto">
  <name>Task 2: Create apollo/synth/spec.py — FM single source of truth (schema + 3 algorithms + dsp_string)</name>
  <read_first>
    - apollo/tokenizer/vocab.py (lines 1-49 — frozen-dataclass-as-contract + "do not mutate without version bump" docstring; mirror this)
    - apollo/ingest/audio.py (lines 35-45 — class-level numeric config constants pattern)
    - .claude/skills/spike-findings-apollo/references/synth-independent-rendering.md (lines 14-38 — proven 2-op FM_DSP template to extend to 3-op)
    - .planning/phases/06-synth-independent-corpus-rendering/06-RESEARCH.md (§"FM Spec Module", §"3-Op Faust Patch", §"Manifest Schema" field ranges, §"Security Domain" — Faust DSP injection)
    - .planning/phases/06-synth-independent-corpus-rendering/06-PATTERNS.md (§"apollo/synth/spec.py" — exact docstring + constant layout)
  </read_first>
  <action>
    Create `apollo/synth/spec.py`. Mirror vocab.py's "single source of truth, do not mutate without bumping SPEC_VERSION" docstring (any change can re-render an already-authored corpus differently — breaks determinism reproducibility).

    Define:
    - `SPEC_VERSION = "1.0"` (str; written into every manifest).
    - `class Algorithm(IntEnum)` with EXACTLY 3 members: `STACK = 0` (3->2->1 chained modulation), `PARALLEL_MODS = 1` ((2+3)->1 two modulators summed into one carrier), `CARRIER_PAIR = 2` (3->1 plus op2 as an independent additive carrier).
    - Determinism-critical engine constants (group with a "spike 001" comment): `SR = 44100`, `BLOCK = 512`, `NUM_VOICES = 8`, `TARGET_PEAK = 0.89` (~ -1 dBFS headroom, Assumption A2).
    - `@dataclass(frozen=True) class OperatorParams` with fields `ratio: float, level: float, attack: float, decay: float, sustain: float, release: float`.
    - `@dataclass(frozen=True) class FmParams` holding `algorithm: int`, `operators: tuple[OperatorParams, ...]` (exactly 3), `gain: float`. Use tuples (not lists) so the dataclass stays hashable/frozen.
    - `def dsp_string(p: FmParams) -> str`: returns deterministic Faust DSP source. Select one of 3 templates by `p.algorithm` (STACK / PARALLEL_MODS / CARRIER_PAIR). Each operator gets its OWN `en.adsr(attack, decay, sustain, release, gate)` envelope and its own `ratio`/`level` slider (per-op ADSR is what makes FM expressive — RESEARCH §"3-Op Faust Patch"). Keep `freq`/`gain`/`gate` as MIDI-owned hsliders/button (NOT settable). End every template with a single mono `_` output (mono per RESEARCH — halves file size, removes L/R asymmetry as a determinism variable). CRITICAL SECURITY (RESEARCH §"Security Domain" — Faust DSP injection): substitute ONLY numeric params via f-string/format of float values; NEVER interpolate any string field from a manifest into the DSP source. The manifest carries numbers/ints only.

    Document the per-op slider NAMES dsp_string emits (e.g. `op{i}_ratio`, `op{i}_level`, `op{i}_attack`, ...) so render.py (Plan 06-02) can build a name->index map at runtime. Do NOT hardcode a static PARAM_INDEX literal — indices are resolved from `get_parameters_description()` at render time (spike landmine).
  </action>
  <verify>
    <automated>.venv/bin/python -c "from apollo.synth.spec import SPEC_VERSION, Algorithm, FmParams, OperatorParams, dsp_string; assert len(Algorithm)==3; p=FmParams(algorithm=0, operators=tuple(OperatorParams(1.0,1.0,0.005,0.12,0.7,0.2) for _ in range(3)), gain=0.5); s=dsp_string(p); assert 'os.osc' in s and 'en.adsr' in s; print('spec OK', SPEC_VERSION)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'SPEC_VERSION = "1.0"' apollo/synth/spec.py`.
    - `.venv/bin/python -c "from apollo.synth.spec import Algorithm; assert len(Algorithm)==3"` exits 0 (exactly 3 algorithms — v1 is 3-op, not 4-op/11-alg).
    - `.venv/bin/python -c "from apollo.synth.spec import SR,BLOCK,NUM_VOICES,TARGET_PEAK; assert (SR,BLOCK,NUM_VOICES)==(44100,512,8) and TARGET_PEAK<1.0"` exits 0.
    - The Task-2 verify command prints `spec OK 1.0` (dsp_string returns Faust containing `os.osc` and `en.adsr`).
    - `grep -q 'frozen=True' apollo/synth/spec.py` (FmParams + OperatorParams frozen).
    - `grep -q 'def dsp_string(p: FmParams) -> str' apollo/synth/spec.py` (signature takes only numeric FmParams — no string field reaches the DSP).
  </acceptance_criteria>
  <done>spec.py defines SPEC_VERSION, a 3-member Algorithm enum, frozen FmParams/OperatorParams, engine constants, and a dsp_string() that emits valid Faust from numeric params only.</done>
</task>

<task type="auto">
  <name>Task 3: Create apollo/synth/manifest.py (validator) + apollo/synth/__init__.py</name>
  <read_first>
    - apollo/ingest/midi.py (lines 29-120 — module-level cap/tolerance constants with decision comments; parse-then-validate-then-raise-IngestError structure to mirror)
    - apollo/ingest/errors.py (IngestError signature — reuse, do not redefine)
    - apollo/ingest/__init__.py (docstring -> re-export -> __all__ shape to mirror for the synth package init)
    - apollo/synth/spec.py (SPEC_VERSION, FmParams, OperatorParams, Algorithm — created in Task 2)
    - .planning/phases/06-synth-independent-corpus-rendering/06-RESEARCH.md (§"Manifest Schema" field ranges table, §"Security Domain" threat patterns)
    - .planning/phases/06-synth-independent-corpus-rendering/06-PATTERNS.md (§"apollo/synth/manifest.py" + §"apollo/synth/__init__.py")
  </read_first>
  <action>
    Create `apollo/synth/manifest.py`. Mirror midi.py's module-level constants + parse->validate->raise discipline.

    Module-level constants (with decision-citing comments, mirroring midi.py): `N_OPERATORS = 3`, `RATIO_MIN, RATIO_MAX = 0.5, 12.0`, `LEVEL_MIN, LEVEL_MAX = 0.0, 1.0`, `GAIN_MIN, GAIN_MAX = 0.0, 1.0`, `ADSR_MIN, ADSR_MAX = 0.0, 2.0` (seconds), `SUSTAIN_MIN, SUSTAIN_MAX = 0.0, 1.0`.

    `def load_manifest(path: str, pair_path: str) -> FmParams`:
    1. `try: raw = json.loads(Path(path).read_text()) except Exception as e: raise IngestError(pair_path, f"failed to parse {path}: {e}")`.
    2. Version guard: `if raw.get("spec_version") != SPEC_VERSION: raise IngestError(pair_path, ...)` naming both values (forward-compat: reject unknown versions so an old corpus can't silently re-render under a changed spec).
    3. Algorithm guard: `algorithm = raw.get("algorithm")`; must be an int in `0..len(Algorithm)-1` else IngestError.
    4. Operator-count guard: `ops = raw.get("operators", [])`; `if len(ops) != N_OPERATORS: raise IngestError(pair_path, f"expected exactly {N_OPERATORS} operators, got {len(ops)}")`.
    5. Per-field validation for each op and for `gain`: every numeric field must be (a) present, (b) a real finite number (reject NaN/Inf via `math.isfinite`), (c) within its documented range. Any violation -> `IngestError(pair_path, "<message naming the offending field>")`. Ranges: ratio in [0.5,12], level in [0,1], attack/decay/release in [0,2], sustain in [0,1], gain in [0,1].
    6. On success, construct and return `FmParams(algorithm=..., operators=tuple(OperatorParams(...) for each op), gain=...)`.

    SECURITY (RESEARCH §"Security Domain"): manifest values are numbers/ints only; never accept or pass through a string that could reach the DSP. NaN/Inf must be rejected (Tampering mitigation).

    Then create `apollo/synth/__init__.py` mirroring `apollo/ingest/__init__.py`: docstring listing the public surface, re-export `SPEC_VERSION, Algorithm, FmParams, dsp_string` (from `.spec`) and `load_manifest` (from `.manifest`), and declare `__all__`. Add a comment noting `render`/`render_call_wav` will be added in Plan 06-02 — do NOT import the not-yet-existent `.render` module.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q 2>&1 | tail -3 && .venv/bin/python tests/_tmp_manifest_check.py 2>/dev/null || .venv/bin/python -c "import json,tempfile,os; from apollo.synth.manifest import load_manifest; from apollo.ingest.errors import IngestError; d=tempfile.mkdtemp(); p=os.path.join(d,'call_fm.json'); ops=[{'ratio':1.0,'level':1.0,'attack':0.005,'decay':0.1,'sustain':0.7,'release':0.2}]*3; open(p,'w').write(json.dumps({'spec_version':'9.9','algorithm':0,'operators':ops,'gain':0.5}));
try:
    load_manifest(p,d); raise SystemExit('FAIL: bad version not rejected')
except IngestError:
    print('OK rejects bad spec_version')"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "from apollo.synth import SPEC_VERSION, Algorithm, FmParams, dsp_string, load_manifest"` exits 0 (public surface exported; no import of .render).
    - A manifest with `spec_version` != "1.0" raises `IngestError` (verify command prints `OK rejects bad spec_version`).
    - A manifest with 2 or 4 operators raises `IngestError` (`.venv/bin/python -c "..."` constructing 2 ops asserts `pytest.raises`-style — at minimum the verify command path covers count rejection conceptually; executor adds a count test).
    - A manifest with `ratio` out of [0.5,12] raises `IngestError`.
    - A manifest with NaN in any field raises `IngestError` (math.isfinite guard present: `grep -q 'isfinite' apollo/synth/manifest.py`).
    - `grep -q 'N_OPERATORS = 3' apollo/synth/manifest.py`.
    - Existing suite still green: `.venv/bin/python -m pytest -q` exits 0.
  </acceptance_criteria>
  <done>manifest.py validates version + exactly-3-operators + finite-and-in-range params (fail-loud IngestError) and returns FmParams; synth/__init__.py exports the spec+manifest surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author/disk -> manifest.py | Hand-authored `call_fm.json` is untrusted input crossing into the renderer's parameter space |
| manifest params -> spec.dsp_string | Numeric params flow into Faust DSP source generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | apollo/synth/manifest.py | mitigate | Strict parse: type-check + `math.isfinite` (reject NaN/Inf) + range-check every field; `IngestError(pair_path, ...)` on any violation (fail-loud, names the pair). |
| T-06-02 | Tampering | apollo/synth/manifest.py (version) | mitigate | Reject any `spec_version` != `SPEC_VERSION` — old corpus cannot silently re-render under a changed spec. |
| T-06-03 | Tampering / Injection | apollo/synth/spec.py dsp_string | mitigate | `dsp_string(p: FmParams)` substitutes numeric params ONLY; no string field from a manifest is ever interpolated into the Faust DSP source. |
| T-06-04 | DoS | apollo/synth/manifest.py (op count) | mitigate | Reject manifests with != 3 operators (bounds the param surface; full DoS render cap is enforced in Plan 06-02 render.py). |
| T-06-05 | Information Disclosure | dawdreamer install | accept | Local-only dependency install; no network data exfil in scope. Rationale: trusted-local workflow per project posture. |
</threat_model>

<verification>
- `.venv/bin/python -c "import torch, dawdreamer"` succeeds (A4 coexistence).
- `.venv/bin/python -c "from apollo.synth import SPEC_VERSION, Algorithm, FmParams, dsp_string, load_manifest"` exits 0.
- `.venv/bin/python -m pytest -q` exits 0 (no regression to the existing 159-test suite).
- Malformed manifests (bad version, wrong op count, out-of-range, NaN) each raise `IngestError`.
</verification>

<success_criteria>
- dawdreamer==0.8.3 pinned and importable alongside torch in one project venv.
- `apollo/synth/spec.py` is the single source of truth: SPEC_VERSION, 3-member Algorithm enum, frozen FmParams/OperatorParams, engine constants, numeric-only dsp_string().
- `apollo/synth/manifest.py` fails loud on every malformed/out-of-range/wrong-version/wrong-count manifest.
- `apollo/synth/__init__.py` exports the spec + manifest public surface.
</success_criteria>

<output>
After completion, create `.planning/phases/06-synth-independent-corpus-rendering/06-01-SUMMARY.md`. Record the A4 outcome (did dawdreamer + torch coexist cleanly in one venv?), the final operator slider naming scheme dsp_string emits, and any deviation from the proposed manifest ranges.

A4 ESCALATION (Task 1, Medium risk): if the combined `torch` + `dawdreamer` import FAILS in the single `.venv`, do NOT silently end the phase. STOP and escalate to the developer for a venv-isolation decision (e.g. an isolated render venv invoked as a subprocess by render_corpus.py, vs. pinning a different dawdreamer/torch wheel combo). Record the exact ABI/wheel error and the chosen path in the SUMMARY so Plans 06-02/06-03 can adjust their interpreter assumption before they run.
</output>
