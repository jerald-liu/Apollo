---
phase: 03-corpus-inference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/pairs/.gitkeep
  - data/pairs/CORPUS-CONVENTIONS.md
autonomous: true
requirements:
  - DATA-05
must_haves:
  truths:
    - "data/pairs/ exists as a tracked directory in the repo"
    - "Authoring conventions are documented so the human can produce conforming pairs"
    - "Conventions cover BPM, preset variety, key/scale, gesture length, file naming, and the ≥30 minimum"
  artifacts:
    - path: "data/pairs/.gitkeep"
      provides: "Tracked empty directory so train.py has a path to point at"
    - path: "data/pairs/CORPUS-CONVENTIONS.md"
      provides: "Human authoring guide for Ableton pair creation"
      contains: "120 BPM"
  key_links:
    - from: "data/pairs/CORPUS-CONVENTIONS.md"
      to: "apollo/ingest/midi.py"
      via: "120 BPM ±2 tolerance enforced by load_notes"
      pattern: "120"
---

<objective>
Create the `data/pairs/` directory stub and `CORPUS-CONVENTIONS.md` authoring guide so the human can populate ≥30 conforming Ableton Operator call/response pairs (DATA-05).

Purpose: The ≥30 pairs themselves are human Ableton work, but the code deliverable for DATA-05 is the directory structure + a written authoring guide that captures all locked decisions (D-01..D-07, DATA-02) so the user knows exactly what to author.

Output: Tracked `data/pairs/` directory + a conventions document referenced by Phase 3 and Phase 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-corpus-inference/03-CONTEXT.md
@.planning/phases/03-corpus-inference/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data/pairs/ directory stub and CORPUS-CONVENTIONS.md authoring guide</name>
  <files>data/pairs/.gitkeep, data/pairs/CORPUS-CONVENTIONS.md</files>
  <read_first>
    - .planning/phases/03-corpus-inference/03-CONTEXT.md (decisions D-01..D-07)
    - .planning/phases/03-corpus-inference/03-RESEARCH.md (§Corpus Authoring Conventions table)
    - .planning/REQUIREMENTS.md (DATA-01, DATA-02, DATA-05 wording)
    - apollo/ingest/midi.py (confirm ±2 BPM tolerance default and load_notes signature)
  </read_first>
  <action>
1. Create the directory `data/pairs/` if it does not exist.

2. Create empty file `data/pairs/.gitkeep` (zero bytes) so the directory is tracked by git.

3. Create `data/pairs/CORPUS-CONVENTIONS.md` with the following EXACT sections and content (substitute concrete values from the table, not placeholders):

```markdown
# Apollo v1 Corpus Authoring Conventions

This directory holds hand-authored Ableton Operator call/response pairs that train the Apollo v1 model. Read this before authoring any pair.

## Minimum corpus size

At least **30 pairs** must exist in this directory before the first real training run (`apollo/scripts/train.py`). This is requirement **DATA-05**.

## Per-pair file layout (DATA-02)

Each pair lives at `data/pairs/NNN/` with exactly three files:

- `call.mid` — MIDI of the call phrase (one Operator MIDI track)
- `call.wav` — Manual Ableton bounce of `call.mid` through its Operator preset
- `response.mid` — MIDI of the response phrase (one Operator MIDI track)

`NNN` is zero-padded sequential (`001`, `002`, ..., `030`, ...).

## Authoring conventions (locked decisions)

| Convention | Value | Source |
|---|---|---|
| Tempo | **120 BPM** exactly. The ingest pipeline enforces ±2 BPM tolerance (`apollo/ingest/midi.py` `load_notes`). | D-01 |
| Operator preset | **Vary across pairs** — bright plucks, soft pads, percussive FM tones, etc. The model must learn that the right response depends on timbre. | D-02 |
| Key / scale | **Free choice per pair.** The tokenizer uses absolute pitch bins, so key variety is not a structural problem. | D-03 |
| Gesture length | **0.5–1.5 seconds, 2–6 notes per side.** Fits within `max_seq_len=64`. | D-04 |
| Response relationship | **Varied per pair** — echo, contrast, continuation, complement. No labeling required. | D-05 |
| Call vs. response preset | **Same or different is free choice per pair.** No constraint. | D-06 |
| Pitch-shift augmentation | **Deferred.** Do not author pitch-shifted variants. | D-07 |

## Authoring workflow (Ableton)

1. Create two MIDI tracks in Ableton. Set both to an Operator preset (preset choice varies per pair, see D-02).
2. Record the **call** phrase on track 1 (2–6 notes, 0.5–1.5s, at 120 BPM).
3. Record the **response** phrase on track 2 (2–6 notes, 0.5–1.5s, at 120 BPM).
4. Solo track 1, **bounce to audio** → save the resulting wav as `call.wav` inside `data/pairs/NNN/`.
5. Export the call MIDI clip as `call.mid` inside `data/pairs/NNN/`.
6. Export the response MIDI clip as `response.mid` inside `data/pairs/NNN/`.
7. Verify: `data/pairs/NNN/` now contains exactly `call.mid`, `call.wav`, `response.mid`.

## Validation

The ingest pipeline (`apollo/scripts/ingest_corpus.py`) will fail loudly on any non-conforming pair:

- Missing files → `IngestError` reports the offending pair
- Tempo outside 120 ± 2 BPM → `IngestError`
- Empty MIDI (zero notes) → `IngestError`
- Polyphony (overlapping notes on same track) → `IngestError`

Run `venv/bin/python -m apollo.scripts.ingest_corpus data/pairs/` to validate the corpus after authoring.
```

4. Do NOT create any subdirectories under `data/pairs/` (those are populated by the human authoring step).

5. Verify `.gitignore` (read it first) does not exclude `data/pairs/` — if it does, do NOT modify .gitignore in this task; flag in the summary instead.
  </action>
  <verify>
    <automated>test -d data/pairs && test -f data/pairs/.gitkeep && test -f data/pairs/CORPUS-CONVENTIONS.md && grep -q "120 BPM" data/pairs/CORPUS-CONVENTIONS.md && grep -q "DATA-05" data/pairs/CORPUS-CONVENTIONS.md && grep -q "DATA-02" data/pairs/CORPUS-CONVENTIONS.md && grep -q "0.5–1.5" data/pairs/CORPUS-CONVENTIONS.md && grep -q "call.mid" data/pairs/CORPUS-CONVENTIONS.md && grep -q "call.wav" data/pairs/CORPUS-CONVENTIONS.md && grep -q "response.mid" data/pairs/CORPUS-CONVENTIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `data/pairs/` exists as a directory
    - `data/pairs/.gitkeep` exists (file, ok zero bytes)
    - `data/pairs/CORPUS-CONVENTIONS.md` exists
    - File contains literal strings: "120 BPM", "DATA-05", "DATA-02", "0.5–1.5", "call.mid", "call.wav", "response.mid"
    - File contains references to decisions D-01 through D-07
    - File documents the Ableton authoring workflow as numbered steps
  </acceptance_criteria>
  <done>data/pairs/ is a tracked directory with a CORPUS-CONVENTIONS.md authoring guide capturing all locked corpus decisions (D-01..D-07) and DATA-02 file layout.</done>
</task>

</tasks>

<verification>
- `data/pairs/` directory exists and is tracked via `.gitkeep`
- `CORPUS-CONVENTIONS.md` documents BPM (120), preset variety, gesture length (0.5–1.5s, 2–6 notes), file naming (NNN), and ≥30 minimum
- Ableton authoring workflow is captured as numbered steps
- All locked decisions D-01..D-07 are referenced
</verification>

<success_criteria>
DATA-05 directory deliverable complete: human can read CORPUS-CONVENTIONS.md and author conforming pairs without asking clarifying questions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-corpus-inference/03-01-SUMMARY.md`
</output>
