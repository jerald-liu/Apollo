---
phase: 07-synth-automation-lfo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apollo/synth/spec.py
  - apollo/synth/manifest.py
  - apollo/synth/render.py
autonomous: true
requirements: [SYNTH-01]
must_haves:
  truths:
    - "SPEC_VERSION is '1.1' and spec.py defines LfoWave/LfoTarget IntEnums + a frozen LfoParams; FmParams gains a trailing optional lfo field defaulting to None"
    - "dsp_string(p) returns the EXACT Phase-6 v1.0 string when p.lfo is None (backward-compat by emitting the same source, not depth-0 wiring)"
    - "When p.lfo is present, dsp_string wires the LFO using NUMERIC values only — select3(int(lfo_wave), os.osc, os.lf_triangle, os.lf_squarewave) and :.6f float hsliders; NO manifest string ever reaches the DSP source"
    - "load_manifest accepts spec_version in {'1.0','1.1'}; a '1.0' manifest carrying an lfo block is rejected with a message naming '1.1'"
    - "Every lfo field is fail-loud validated: rate/depth via _check_number (finite, in-range, reject bools), wave/target via integer-enum check; each rejection raises IngestError(pair_path, reason)"
    - "render.py sets lfo_rate/lfo_depth/lfo_wave by runtime label->index resolution ONLY when params.lfo is not None (no-lfo patch never looks up absent sliders)"
  artifacts:
    - path: "apollo/synth/spec.py"
      provides: "SPEC_VERSION='1.1', LfoWave/LfoTarget IntEnums, frozen LfoParams, FmParams.lfo optional field, dsp_string LFO branch (numeric-only)"
      contains: "SPEC_VERSION = \"1.1\""
      min_lines: 200
    - path: "apollo/synth/manifest.py"
      provides: "SUPPORTED_VERSIONS={'1.0','1.1'}, _check_enum_int, LFO bounds constants, optional-lfo validation block, lfo-requires-1.1 cross-check"
      contains: "_check_enum_int"
      min_lines: 150
    - path: "apollo/synth/render.py"
      provides: "runtime set of lfo_rate/lfo_depth/lfo_wave by label->index, guarded on params.lfo is not None"
      contains: "params.lfo is not None"
      min_lines: 200
  key_links:
    - from: "apollo/synth/manifest.py"
      to: "apollo/synth/spec.py"
      via: "imports LfoParams, LfoTarget, LfoWave for validation + construction"
      pattern: "from apollo.synth.spec import"
    - from: "apollo/synth/manifest.py"
      to: "apollo/ingest/errors.py"
      via: "raises IngestError(pair_path, reason) on every lfo validation failure"
      pattern: "IngestError"
    - from: "apollo/synth/render.py"
      to: "apollo/synth/spec.py dsp_string"
      via: "set_dsp_string(dsp_string(params)) — LFO sliders emitted only when lfo present"
      pattern: "dsp_string\\(params\\)"
---

<objective>
Extend the owned 3-op FM synth with one optional global LFO, bumping the FM spec to v1.1 — backward-compatible, deterministic, and numeric-only. This is the code core of Phase 7: the three coordinated `apollo/synth/` edits that define the v1.1 contract every downstream test and doc consumes.

- `spec.py`: bump `SPEC_VERSION` to `"1.1"`; add `LfoWave`/`LfoTarget` IntEnums + frozen `LfoParams`; add `lfo: LfoParams | None = None` to `FmParams`; branch `dsp_string` so it emits the EXACT Phase-6 v1.0 source when `lfo is None` (RESEARCH Pitfall 1 — strictly safer than depth-0 wiring) and adds a numeric-only LFO block when `lfo` is present.
- `manifest.py`: relax the version guard to accept `{"1.0","1.1"}`, add the `lfo`-requires-`"1.1"` cross-check, add an integer-enum checker, and validate every lfo field fail-loud (reuse `_check_number` for rate/depth).
- `render.py`: set the three LFO sliders by runtime label->index resolution, guarded on `params.lfo is not None` (RESEARCH Pitfall 3 — the no-lfo path must never look up absent sliders).

Purpose: deliver SYNTH-01's mechanism (time-varying timbre/pitch) without disturbing the Phase-6 bit-identity guarantee. Phase 5's browser synth mirrors this exact schema.
Output: the three extended modules in `apollo/synth/`.

**Orchestrator working defaults baked in (NOT user-locked — overridable at execute time; no CONTEXT.md exists for this phase):**
- ONE global LFO per patch (not per-operator) for the v1.1 minimal slice.
- Waveforms `{0:sine, 1:triangle, 2:square}` (validated integer enum; reserve `3=saw` slot, do NOT implement).
- Targets `{0:LEVEL (tremolo), 1:PITCH (vibrato)}` — both RESEARCH-validated; reserve `2=FM-mod-depth` slot, do NOT implement.
- Rate `0.05–20` Hz; depth `0–1`.
- LFO absent ⇒ emit the verbatim Phase-6 v1.0 DSP string.
- **Vibrato max-cents = 50** (RESEARCH A1 recommended default for musical vibrato; baked as a Faust numeric literal; discuss-phase could refine to 100 for dramatic). Mark this in a code comment as an orchestrator default.
- **CARRIER_PAIR LFO application: ALL carriers uniformly** (RESEARCH A4 recommended default — symmetric, simplest, one global LFO ⇒ both carriers move together; discuss-phase could refine to first-carrier-only). Mark this in a code comment as an orchestrator default.
- LFO phase is per-note/onset-relative starting at 0 — `os.osc`/`os.lf_*` reset per poly voice at note-on (RESEARCH VERIFIED); NO special phase handling needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase design + validated patterns (READ before implementing)
@.planning/phases/07-synth-automation-lfo/07-RESEARCH.md
@.planning/phases/07-synth-automation-lfo/07-PATTERNS.md
@.claude/skills/spike-findings-apollo/SKILL.md

<interfaces>
<!-- Contracts to satisfy / reuse. Use directly — no exploration needed. -->

IngestError (apollo/ingest/errors.py) — REUSE, do not redefine:
```python
class IngestError(Exception):
    def __init__(self, pair_path: str, reason: str): ...   # formats as f"[{pair_path}] {reason}"
```

Current spec.py shapes to EXTEND (do not rewrite — surgical edits):
- `SPEC_VERSION = "1.0"` (line 37) → `"1.1"`.
- `from enum import IntEnum` already imported (line 32).
- `class Algorithm(IntEnum)` STACK/PARALLEL_MODS/CARRIER_PAIR (lines 40-51) — unchanged.
- `@dataclass(frozen=True) class OperatorParams` (lines 54-75) — unchanged.
- `@dataclass(frozen=True) class FmParams` (lines 78-88): add `lfo: LfoParams | None = None` as the TRAILING field so positional v1.0 constructor calls (`FmParams(algorithm=..., operators=..., gain=...)`) and existing helpers keep working.
- `dsp_string(p)` (lines 100-177): three templates select carrier names `car` (STACK lines 152-159 / PARALLEL_MODS 160-167) and `car1`/`car2` (CARRIER_PAIR 168-175). Float substitution is `{float(x):.6f}` (lines 134-143).

Current manifest.py shapes to EXTEND:
- imports `from apollo.synth.spec import SPEC_VERSION, Algorithm, FmParams, OperatorParams` (line 36) — ADD `LfoParams, LfoTarget, LfoWave`.
- bounds constants block (lines 38-61) — add `LFO_RATE_MIN/MAX = 0.05, 20.0` and `LFO_DEPTH_MIN/MAX = 0.0, 1.0`.
- `_check_number(value, pair_path, label, lo, hi)` (lines 64-77) — REUSE for rate/depth; already rejects bools, non-numeric, NaN/Inf, out-of-range.
- algorithm guard (lines 104-109) is the existing "bool-reject + int-check + membership-check + IngestError" analog for the new `_check_enum_int`.
- version guard (lines 95-101) is exact-equality `version != SPEC_VERSION` — RELAX to set membership `{"1.0","1.1"}`, KEEP the message containing `spec_version` (test_manifest_bad_version asserts `match="spec_version"` for "9.9").
- gain block (lines 131-133) then `return FmParams(...)` (line 135) — insert the lfo block before the return, thread `lfo=lfo` into FmParams.

Current render.py shapes to EXTEND:
- `_build_name_index_map(synth)` (lines 77-97) resolves slider label->int index — `lfo_rate`/`lfo_depth`/`lfo_wave` resolve through it UNCHANGED (RESEARCH VERIFIED). Do NOT add a static index map.
- per-op slider-set loop (lines 137-145) sets `op{i}_{field}` by int index with a missing-name guard — MIRROR exactly for the lfo block.
- `_OP_SLIDER_FIELDS = ("ratio", "level")` (line 74) — unchanged. `lfo.target` is NOT a runtime slider (it selects the DSP branch at compile time, like algorithm) — do NOT set it here.

Faust LFO block (VERIFIED to compile + render this session, dawdreamer 0.8.3):
```faust
lfo_rate  = hslider("lfo_rate",  6.0, 0.05, 20, 0.001);
lfo_depth = hslider("lfo_depth", 0.0, 0,    1,  0.001);
lfo_wave  = hslider("lfo_wave",  0,   0,    2,  1);
lfo_bi = select3(int(lfo_wave), os.osc(lfo_rate), os.lf_triangle(lfo_rate), os.lf_squarewave(lfo_rate));
// LEVEL target (tremolo): unipolar gain in [1-depth, 1]
lfo_uni = (lfo_bi + 1.0) / 2.0;
lvl_mod = 1.0 - lfo_depth * (1.0 - lfo_uni);   // multiply each carrier by lvl_mod
// PITCH target (vibrato): pitch_mul = pow(2.0, (lfo_bi * lfo_depth * 50.0) / 1200.0);   // 50 cents (orchestrator default)
//   apply by multiplying each carrier oscillator's freq argument by pitch_mul
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend spec.py — bump to 1.1, add LFO schema, branch dsp_string (numeric-only)</name>
  <read_first>
    - apollo/synth/spec.py (FULL FILE — the file being modified; SPEC_VERSION line 37, FmParams lines 78-88, dsp_string lines 100-177, carrier names in each template lines 152-175, float-substitution convention lines 134-143)
    - .planning/phases/07-synth-automation-lfo/07-PATTERNS.md (§"apollo/synth/spec.py" — exact enum/dataclass/branch placement + numeric-only rule)
    - .planning/phases/07-synth-automation-lfo/07-RESEARCH.md (§"Architecture Patterns" Pattern 1/2/3, §"Common Pitfalls" Pitfall 1, §"Security Domain")
  </read_first>
  <behavior>
    - dsp_string(FmParams(..., lfo=None)) returns a string byte-for-byte equal to the current Phase-6 dsp_string output for the same algorithm+operators+gain (no LFO ops emitted at all).
    - dsp_string(FmParams(..., lfo=LfoParams(rate=6.0, depth=0.8, wave=0, target=0))) contains `select3(int(lfo_wave)`, `os.lf_triangle`, `os.lf_squarewave`, `lfo_rate`, `lfo_depth`, `lfo_wave`, and applies `lvl_mod` to the carrier(s).
    - dsp_string with target=1 (PITCH) emits `pow(2` and multiplies carrier freq args by the vibrato multiplier (50.0 cents literal present).
    - For CARRIER_PAIR (algorithm=2) with an LFO, BOTH car1 and car2 receive the modulation.
    - No Python branch on a string field name; waveform selection is purely `select3(int(lfo_wave), ...)`.
  </behavior>
  <action>
    Surgical edits to `apollo/synth/spec.py` (mirror the existing module's IntEnum + frozen-dataclass + numeric-only-dsp discipline; do NOT rewrite untouched code):

    1. Bump the version constant: `SPEC_VERSION = "1.1"` (was `"1.0"`). Update the surrounding doc comment to note v1.1 adds an optional LFO and that v1.0 manifests still render bit-identically.

    2. Add two IntEnums (mirror `Algorithm`) above `OperatorParams`:
       ```python
       class LfoWave(IntEnum):
           SINE = 0
           TRIANGLE = 1
           SQUARE = 2
           # reserve 3 = SAW for a later spec bump — do NOT implement in v1.1

       class LfoTarget(IntEnum):
           LEVEL = 0   # tremolo (scales carrier output)
           PITCH = 1   # vibrato (scales carrier frequency in cents)
           # reserve 2 = FM-mod-depth for a later spec bump — do NOT implement in v1.1
       ```

    3. Add a frozen dataclass (mirror `OperatorParams`):
       ```python
       @dataclass(frozen=True)
       class LfoParams:
           rate: float       # Hz, [0.05, 20.0]
           depth: float      # [0.0, 1.0]
           wave: int         # LfoWave value
           target: int       # LfoTarget value
       ```

    4. Add `lfo: LfoParams | None = None` as the TRAILING field of `FmParams` (after `gain`) so existing positional/keyword constructor calls and the v1.0 helpers are unchanged and the dataclass stays frozen/hashable.

    5. Branch `dsp_string`. At the very top of the body (after the algorithm/operator-count guards), add:
       `if p.lfo is None: return <the exact existing v1.0 body, unchanged>`.
       The cleanest implementation: factor the current header+body construction so the no-lfo path returns precisely today's string (RESEARCH Pitfall 1 — bit-identity is preserved by emitting the SAME source). When `p.lfo is not None`, additionally emit the LFO declarations (numeric-only):
       - `lfo_rate = hslider("lfo_rate", {float(p.lfo.rate):.6f}, 0.05, 20, 0.001);`
       - `lfo_depth = hslider("lfo_depth", {float(p.lfo.depth):.6f}, 0, 1, 0.001);`
       - `lfo_wave = hslider("lfo_wave", {int(p.lfo.wave)}, 0, 2, 1);`  (integer-step hslider; emit as int)
       - `lfo_bi = select3(int(lfo_wave), os.osc(lfo_rate), os.lf_triangle(lfo_rate), os.lf_squarewave(lfo_rate));`
       Then apply the target (branch on `int(p.lfo.target)` in PYTHON to choose WHICH wiring to emit — this is numeric, like algorithm; it does NOT interpolate a string):
       - target == LfoTarget.LEVEL: emit `lfo_uni = (lfo_bi + 1.0) / 2.0;` and `lvl_mod = 1.0 - lfo_depth * (1.0 - lfo_uni);`, then multiply EACH carrier expression by `* lvl_mod` (STACK/PARALLEL_MODS: `car`; CARRIER_PAIR: both `car1` AND `car2` — orchestrator default A4, mark with a comment).
       - target == LfoTarget.PITCH: emit `pitch_mul = pow(2.0, (lfo_bi * lfo_depth * 50.0) / 1200.0);` (50 cents — orchestrator default A1, mark with a comment) and multiply EACH carrier oscillator's freq argument by `pitch_mul` (e.g. `os.osc((freq * op1_ratio + mod2) * pitch_mul)`; CARRIER_PAIR applies to both carriers' freq args).
       CRITICAL SECURITY (RESEARCH §"Security Domain", T-06-01 class): substitute ONLY numeric/int values via `:.6f` / `int(...)`. NEVER interpolate `LfoWave`/`LfoTarget` names or any string into the DSP source — waveform is selected by `select3(int(lfo_wave), ...)`, target by a Python branch choosing which numeric wiring to emit.

    Update the module docstring's "do not mutate without bumping" note to mention the optional LFO block + the v1.0/v1.1 acceptance.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from apollo.synth.spec import SPEC_VERSION, LfoWave, LfoTarget, LfoParams, FmParams, OperatorParams, dsp_string; assert SPEC_VERSION=='1.1'; assert (LfoWave.SINE,LfoWave.TRIANGLE,LfoWave.SQUARE)==(0,1,2); assert (LfoTarget.LEVEL,LfoTarget.PITCH)==(0,1); op=OperatorParams(1.0,0.8,0.005,0.1,0.6,0.2); base=FmParams(algorithm=0,operators=(op,op,op),gain=0.5); s0=dsp_string(base); lp=FmParams(algorithm=0,operators=(op,op,op),gain=0.5,lfo=LfoParams(6.0,0.8,0,0)); s1=dsp_string(lp); assert 'select3(int(lfo_wave)' in s1 and 'os.lf_squarewave' in s1 and 'lvl_mod' in s1; assert 'lfo_' not in s0; sp=FmParams(algorithm=0,operators=(op,op,op),gain=0.5,lfo=LfoParams(6.0,0.8,0,1)); assert 'pow(2' in dsp_string(sp); print('spec 1.1 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'SPEC_VERSION = "1.1"' apollo/synth/spec.py` succeeds.
    - `grep -q 'class LfoWave(IntEnum)' apollo/synth/spec.py` and `grep -q 'class LfoTarget(IntEnum)' apollo/synth/spec.py` and `grep -q 'class LfoParams' apollo/synth/spec.py`.
    - `grep -q 'lfo: LfoParams | None = None' apollo/synth/spec.py` (trailing optional field).
    - `grep -q 'select3(int(lfo_wave)' apollo/synth/spec.py` (waveform select is numeric, not string-branched).
    - `grep -q 'os.lf_squarewave' apollo/synth/spec.py` and `grep -q 'os.lf_triangle' apollo/synth/spec.py`.
    - NO manifest string interpolated into DSP: `! grep -nE 'LfoWave\.|LfoTarget\.|\.name' apollo/synth/spec.py | grep -i 'dsp\|hslider\|select3'` (waveform/target dispatch is by int only).
    - The Task-1 verify command prints `spec 1.1 OK` (no-lfo string omits `lfo_`; lfo string has select3+lvl_mod; pitch target has `pow(2`).
    - `.venv/bin/python -m pytest -q tests/test_synth_render.py` exits 0 (existing Phase-6 tests still pass — no-lfo path unchanged; render tests guarded by importorskip).
  </acceptance_criteria>
  <done>spec.py is at SPEC_VERSION "1.1" with LfoWave/LfoTarget/LfoParams, an optional FmParams.lfo, and a dsp_string that emits the verbatim v1.0 string when lfo is None and a numeric-only LFO block (level + pitch targets, all carriers) when present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend manifest.py — accept {1.0,1.1}, lfo-requires-1.1, fail-loud lfo validation</name>
  <read_first>
    - apollo/synth/manifest.py (FULL FILE — imports line 36, bounds constants 38-61, _check_number 64-77, version guard 95-101, algorithm guard 104-109, gain block 131-133, return line 135)
    - apollo/synth/spec.py (LfoParams, LfoWave, LfoTarget — created in Task 1)
    - .planning/phases/07-synth-automation-lfo/07-PATTERNS.md (§"apollo/synth/manifest.py" — _check_enum_int, version-set relax, optional-block placement)
    - .planning/phases/07-synth-automation-lfo/07-RESEARCH.md (§"Code Examples" validation snippet, §"Manifest Schema" field-ranges, §"Security Domain" threat patterns)
  </read_first>
  <behavior>
    - load_manifest on a valid v1.0 manifest (no lfo) returns FmParams with lfo=None (unchanged from Phase 6).
    - load_manifest on a valid v1.1 manifest with an lfo block returns FmParams whose .lfo is an LfoParams with the parsed values.
    - load_manifest on spec_version "9.9" still raises IngestError matching "spec_version" (existing test_manifest_bad_version preserved).
    - load_manifest on spec_version "1.0" + an lfo block raises IngestError matching "1.1".
    - lfo.rate outside [0.05,20.0], lfo.depth outside [0,1], NaN/Inf in rate/depth, a bool in any lfo field, or lfo.wave/lfo.target not in the enum → IngestError naming the offending field and the pair.
  </behavior>
  <action>
    Surgical edits to `apollo/synth/manifest.py` (mirror its existing fail-loud `_check_number` / module-constant / version-guard discipline):

    1. Import: add `LfoParams, LfoTarget, LfoWave` to the existing `from apollo.synth.spec import ...` line.

    2. Bounds constants (near the other `*_MIN/MAX` block, with decision comments):
       `LFO_RATE_MIN, LFO_RATE_MAX = 0.05, 20.0` and `LFO_DEPTH_MIN, LFO_DEPTH_MAX = 0.0, 1.0`.

    3. Add the integer-enum checker (mirror the algorithm guard's bool-reject + int-check + membership-check):
       ```python
       def _check_enum_int(value, pair_path, label, valid: set[int]) -> int:
           if isinstance(value, bool) or not isinstance(value, int):
               raise IngestError(pair_path, f"{label} must be an int, got {type(value).__name__}")
           if value not in valid:
               raise IngestError(pair_path, f"{label} {value} not in {sorted(valid)}")
           return value
       ```

    4. Relax the version guard. Add `SUPPORTED_VERSIONS = {"1.0", "1.1"}` (module constant), and replace the exact-equality check with set membership while KEEPING the message containing the word `spec_version` (test_manifest_bad_version asserts `match="spec_version"` for "9.9"):
       ```python
       version = raw.get("spec_version")
       if version not in SUPPORTED_VERSIONS:
           raise IngestError(pair_path, f"manifest spec_version {version!r} not in {sorted(SUPPORTED_VERSIONS)}")
       ```
       (Keep `version` bound — the lfo cross-check below uses it.)

    5. After the `gain` block and BEFORE `return FmParams(...)`, add the optional-lfo validation (RESEARCH §"Code Examples"):
       ```python
       lfo = None
       raw_lfo = raw.get("lfo")
       if raw_lfo is not None:
           if version != "1.1":
               raise IngestError(pair_path, "lfo block requires spec_version '1.1'")
           if not isinstance(raw_lfo, dict):
               raise IngestError(pair_path, f"lfo must be an object, got {type(raw_lfo).__name__}")
           rate  = _check_number(raw_lfo.get("rate"),  pair_path, "lfo rate",  LFO_RATE_MIN, LFO_RATE_MAX)
           depth = _check_number(raw_lfo.get("depth"), pair_path, "lfo depth", LFO_DEPTH_MIN, LFO_DEPTH_MAX)
           wave   = _check_enum_int(raw_lfo.get("wave"),   pair_path, "lfo wave",   {w.value for w in LfoWave})
           target = _check_enum_int(raw_lfo.get("target"), pair_path, "lfo target", {t.value for t in LfoTarget})
           lfo = LfoParams(rate=rate, depth=depth, wave=wave, target=target)
       ```
       (Missing rate/depth keys → `.get()` returns None → `_check_number` rejects, naming the pair. Missing wave/target → None → `_check_enum_int` rejects.)

    6. Thread `lfo=lfo` into the `return FmParams(algorithm=..., operators=..., gain=..., lfo=lfo)`.

    7. Update the module docstring schema block to document the optional `lfo` field, the `{"1.0","1.1"}` acceptance, and the lfo-requires-1.1 rule.

    SECURITY (RESEARCH §"Security Domain", T-06-01 class): the lfo block carries numbers/ints only; reject bools, NaN/Inf, out-of-range, unknown enum ints. Never accept a string lfo field.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json, tempfile, os
from apollo.synth.manifest import load_manifest, SUPPORTED_VERSIONS
from apollo.ingest.errors import IngestError
d=tempfile.mkdtemp(); p=os.path.join(d,'call_fm.json')
op={'ratio':1.0,'level':0.8,'attack':0.005,'decay':0.1,'sustain':0.6,'release':0.2}
def w(m): open(p,'w').write(json.dumps(m)); return p
base={'spec_version':'1.1','algorithm':0,'operators':[dict(op)]*3,'gain':0.5}
fp=load_manifest(w({**base,'lfo':{'rate':6.0,'depth':0.8,'wave':0,'target':0}}),d); assert fp.lfo is not None and fp.lfo.rate==6.0
assert load_manifest(w({**base})).lfo is None
v10={**base,'spec_version':'1.0'}; assert load_manifest(w(v10)).lfo is None
def rej(m,tok):
    try: load_manifest(w(m),d); raise SystemExit('FAIL not rejected: '+tok)
    except IngestError as e: assert tok in str(e), (tok,str(e))
rej({**v10,'lfo':{'rate':6.0,'depth':0.8,'wave':0,'target':0}}, '1.1')
rej({**base,'lfo':{'rate':99.0,'depth':0.8,'wave':0,'target':0}}, 'lfo rate')
rej({**base,'lfo':{'rate':6.0,'depth':0.8,'wave':9,'target':0}}, 'lfo wave')
rej({**base,'lfo':{'rate':float('nan'),'depth':0.8,'wave':0,'target':0}}, 'finite')
assert SUPPORTED_VERSIONS=={'1.0','1.1'}
print('manifest 1.1 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def _check_enum_int' apollo/synth/manifest.py`.
    - `grep -q "SUPPORTED_VERSIONS = {\"1.0\", \"1.1\"}" apollo/synth/manifest.py` (or equivalent set literal containing both versions).
    - `grep -q 'LFO_RATE_MIN, LFO_RATE_MAX = 0.05, 20.0' apollo/synth/manifest.py`.
    - `grep -q "lfo block requires spec_version '1.1'" apollo/synth/manifest.py`.
    - `grep -q 'from apollo.synth.spec import' apollo/synth/manifest.py` and that line includes `LfoParams`.
    - The Task-2 verify command prints `manifest 1.1 OK` (valid v1.1+lfo loads; v1.0 no-lfo loads; v1.0+lfo / out-of-range rate / bad wave enum / NaN all rejected with the right token).
    - `.venv/bin/python -m pytest -q tests/test_synth_render.py` exits 0 (test_manifest_bad_version "9.9" still rejected with "spec_version" message).
  </acceptance_criteria>
  <done>manifest.py accepts spec_version {1.0,1.1}, rejects lfo under 1.0, and fail-loud validates every lfo field (finite/range/enum/bool), returning FmParams with the parsed lfo (or None).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend render.py — set lfo sliders by runtime index, guarded on params.lfo</name>
  <read_first>
    - apollo/synth/render.py (FULL FILE — _build_name_index_map lines 77-97, per-op set loop lines 137-145, _OP_SLIDER_FIELDS line 74, _normalize_peak lines 168-179)
    - apollo/synth/spec.py (FmParams.lfo, LfoParams — from Task 1)
    - .planning/phases/07-synth-automation-lfo/07-PATTERNS.md (§"apollo/synth/render.py" — mirror the set-loop, guard on lfo is not None, target is NOT a runtime slider)
    - .planning/phases/07-synth-automation-lfo/07-RESEARCH.md (§"Code Examples" render snippet, §"Common Pitfalls" Pitfall 3 + 5, poly param-by-index landmine)
  </read_first>
  <behavior>
    - For a no-lfo patch, render.py sets ONLY op{i}_ratio/op{i}_level (unchanged) — it never looks up lfo_* sliders (which the no-lfo dsp_string does not emit), so no spurious "missing param" error.
    - For an lfo patch, render.py additionally sets lfo_rate, lfo_depth, lfo_wave by their runtime-resolved int index (resolved by label via the existing _build_name_index_map — no static index).
    - lfo.target is NOT set as a runtime slider (it selected the DSP branch at compile time in Task 1).
    - If an lfo patch's engine description is missing lfo_rate/lfo_depth/lfo_wave, render raises IngestError(pair_path, "render engine is missing expected param 'lfo_...'").
  </behavior>
  <action>
    Surgical edit to `apollo/synth/render.py`. Inside `render()`, immediately AFTER the existing per-operator slider-set loop (and inside the same `with contextlib.redirect_stderr(...)` block, using the already-built `name_to_index`), add the LFO set block — mirror the per-op loop's guard-then-set-by-int-index shape exactly:
    ```python
    if params.lfo is not None:
        for slider, val in (("lfo_rate", params.lfo.rate),
                            ("lfo_depth", params.lfo.depth),
                            ("lfo_wave", float(params.lfo.wave))):
            if slider not in name_to_index:
                raise IngestError(pair_path, f"render engine is missing expected param {slider!r}")
            synth.set_parameter(int(name_to_index[slider]), float(val))
    ```
    Guard the WHOLE block on `params.lfo is not None` (RESEARCH Pitfall 3 — when lfo is None the dsp_string emits no lfo sliders, so name_to_index won't contain them; never look them up). Do NOT set `lfo_wave` via any string — pass `float(params.lfo.wave)` (the Faust `select3(int(lfo_wave), ...)` re-discretizes it). Do NOT set `lfo.target` here (it is a compile-time branch). Keep `_OP_SLIDER_FIELDS` unchanged; `_normalize_peak` already handles LFO-induced peak changes deterministically (RESEARCH Pitfall 5 — no change). Add a brief comment citing Pitfall 3 (guard on lfo presence) and the numeric-only rule.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import dawdreamer" 2>/dev/null && .venv/bin/python -c "
import numpy as np, tempfile, os, pretty_midi
from apollo.synth.render import render
from apollo.synth.spec import FmParams, OperatorParams, LfoParams
from apollo.ingest.midi import load_notes
d=tempfile.mkdtemp(); mid=os.path.join(d,'call.mid')
pm=pretty_midi.PrettyMIDI(initial_tempo=120.0); inst=pretty_midi.Instrument(program=0)
for i,s in enumerate([0.0,0.5,1.0]): inst.notes.append(pretty_midi.Note(velocity=100,pitch=60+i,start=s,end=s+0.4))
pm.instruments.append(inst); pm.write(mid)
notes=load_notes(mid,d,tempo_bpm=120.0)
op=OperatorParams(1.0,0.8,0.005,0.1,0.6,0.2)
nolfo=FmParams(algorithm=0,operators=(op,op,op),gain=0.5)
lfo=FmParams(algorithm=0,operators=(op,op,op),gain=0.5,lfo=LfoParams(6.0,0.8,0,0))
a=render(nolfo,notes,pair_path=d); b=render(lfo,notes,pair_path=d)
assert np.array_equal(render(lfo,notes,pair_path=d), b)  # lfo render deterministic
assert not np.array_equal(a,b)  # lfo changes the audio
print('render lfo OK')" || echo 'SKIP: dawdreamer absent (render block still grep-verified)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'params.lfo is not None' apollo/synth/render.py` (the set block is guarded).
    - `grep -q 'lfo_rate' apollo/synth/render.py` and `grep -q 'lfo_depth' apollo/synth/render.py` and `grep -q 'lfo_wave' apollo/synth/render.py`.
    - `grep -q 'render engine is missing expected param' apollo/synth/render.py` (missing-slider guard reused for lfo).
    - lfo.target is NOT set as a runtime param: `! grep -q 'lfo_target' apollo/synth/render.py`.
    - `_OP_SLIDER_FIELDS` unchanged: `grep -q '_OP_SLIDER_FIELDS = ("ratio", "level")' apollo/synth/render.py`.
    - With dawdreamer present, the Task-3 verify command prints `render lfo OK` (lfo render deterministic AND differs from the no-lfo render). On non-arm64 CI it prints the SKIP line.
    - `.venv/bin/python -m pytest -q tests/test_synth_render.py` exits 0.
  </acceptance_criteria>
  <done>render.py sets lfo_rate/lfo_depth/lfo_wave by runtime label->index ONLY when params.lfo is present (target excluded); no-lfo renders are untouched; lfo renders are deterministic and audibly differ.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author/disk -> manifest.py | Hand-authored `call_fm.json` (now optionally carrying an `lfo` block) is untrusted input crossing into the renderer's parameter space |
| manifest lfo params -> spec.dsp_string | Validated numeric lfo params flow into Faust DSP source generation (waveform/target dispatch is numeric-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering / Injection | apollo/synth/spec.py dsp_string | mitigate | Waveform selected via `select3(int(lfo_wave), ...)`; target via a Python branch choosing numeric wiring; rate/depth substituted as `:.6f` floats and wave as `int(...)`. NO manifest string is ever interpolated into the DSP source (Task 1 acceptance greps enforce this). |
| T-07-02 | Tampering | apollo/synth/manifest.py (lfo fields) | mitigate | `_check_number` (finite + range, reject bools) for rate/depth; `_check_enum_int` for wave/target; every rejection raises `IngestError(pair_path, reason)` naming the pair (Task 2). |
| T-07-03 | Tampering / spec confusion | apollo/synth/manifest.py (version) | mitigate | Accept only `spec_version ∈ {"1.0","1.1"}`; reject an `lfo` block under `"1.0"` (version downgrade carrying lfo) with a message naming `"1.1"` (Task 2). |
| T-07-04 | DoS | apollo/synth/manifest.py (rate) + render.py | mitigate | `lfo.rate` capped at 20 Hz; LFO does not extend render length, so the existing `MAX_RENDER_SECONDS` duration cap (render.py) remains the DoS bound — unchanged. |
| T-07-05 | Information Disclosure | local render pipeline | accept | Local-only, in-process render; manifests carry numbers only; no auth/network/PII surface. Rationale: trusted-local workflow per project posture (carried from T-06-05). |
</threat_model>

<verification>
- `.venv/bin/python -c "from apollo.synth import SPEC_VERSION, LfoParams, LfoWave, LfoTarget, FmParams, dsp_string, load_manifest; assert SPEC_VERSION=='1.1'"` exits 0 (package surface re-exports the spec symbols; synth/__init__.py already re-exports from .spec — confirm LfoParams/LfoWave/LfoTarget are reachable, adding them to __all__ if the executor chooses to surface them).
- `dsp_string(no_lfo)` is byte-identical to the Phase-6 output (proved against itself in Task 1 + by the 07-02 bit-identity test).
- All three orchestrator-default values are present and commented as defaults: 50-cent vibrato literal in spec.py; all-carriers comment in spec.py CARRIER_PAIR branch.
- `.venv/bin/python -m pytest -q` exits 0 (full suite green — no regression to the 177 Phase-6 tests).
</verification>

<success_criteria>
- spec.py at SPEC_VERSION "1.1" with LfoWave/LfoTarget/LfoParams + optional FmParams.lfo + numeric-only dsp_string LFO branch (verbatim v1.0 string when lfo is None).
- manifest.py accepts {1.0,1.1}, rejects lfo under 1.0, fail-loud validates every lfo field.
- render.py sets lfo sliders by runtime index, guarded on params.lfo presence (target excluded).
- Existing Phase-6 suite unchanged and green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-synth-automation-lfo/07-01-SUMMARY.md`. Record: the exact LFO Faust block emitted (level + pitch wiring), the vibrato max-cents literal used (50, orchestrator default — note discuss-phase could refine), the CARRIER_PAIR application choice (all carriers, orchestrator default A4), and whether LfoParams/LfoWave/LfoTarget were added to synth/__init__.py __all__. Flag any deviation from the proposed lfo bounds.
</output>
