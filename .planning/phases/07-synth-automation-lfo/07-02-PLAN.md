---
phase: 07-synth-automation-lfo
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - tests/test_synth_render.py
autonomous: true
requirements: [SYNTH-01]
must_haves:
  truths:
    - "A no-lfo v1.1 manifest renders bit-identically to the Phase-6 path (np.array_equal AND dsp_string(no_lfo) emits no lfo_ tokens — same v1.0 source)"
    - "Re-rendering the same v1.1 lfo manifest is bit-identical (np.array_equal across two renders)"
    - "An LFO render produces measurable time-variation vs a depth-0 render through the frozen MelExtractor (cos < 0.999 at 6 Hz)"
    - "A depth-0 lfo render is bit-identical to the static (no-modulation) render (np.array_equal)"
    - "load_manifest accepts a v1.1 manifest; lfo under '1.0' is rejected (match='1.1'); lfo rate-out-of-range / bad-enum / NaN each raise IngestError"
    - "The (96,128) float32 mel contract and the no-clip (<=1.0) invariant both still hold for an lfo patch"
  artifacts:
    - path: "tests/test_synth_render.py"
      provides: "_fm_params/_manifest_dict gain an lfo= kwarg + spec_version override; new LFO determinism/bit-identity/time-variation/validation tests"
      contains: "test_lfo_render_deterministic"
      min_lines: 280
  key_links:
    - from: "tests/test_synth_render.py"
      to: "apollo/synth/spec.py + render.py + manifest.py"
      via: "imports LfoParams/dsp_string/render/load_manifest and exercises the v1.1 LFO contract"
      pattern: "from apollo.synth.spec import|from apollo.synth.manifest import|from apollo.synth.render import"
---

<objective>
Prove the v1.1 LFO contract with tests, exercising every SYNTH-01 guarantee: backward-compat bit-identity, LFO determinism, measurable mel time-variation, depth-0 == static, the relaxed version acceptance, the lfo-requires-1.1 rejection, lfo field validation, and the preserved (96,128) / no-clip invariants. All render tests stay under the existing `pytest.importorskip("dawdreamer")` guard; validation tests run unconditionally. No `.wav` fixture is committed — every render is in-process to `tmp_path` (existing convention).

Purpose: turn the RESEARCH Validation Architecture test map (9 tests) into the executable proof that SYNTH-01 SC#1-3, #5 hold and Phase-6 determinism did not regress.
Output: extended `tests/test_synth_render.py`.

**Orchestrator working defaults (mirror 07-01; NOT user-locked):** time-variation test rate = 6 Hz (RESEARCH measured cos=0.9990/L2=74.11; do NOT use the 0.05 Hz edge — Pitfall 4); waveforms {0,1,2}; targets {0=level,1=pitch}; the bit-identity test asserts `dsp_string(no_lfo)` emits no `lfo_` tokens in-process (no committed golden `.wav`, RESEARCH Wave-0 gap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/07-synth-automation-lfo/07-RESEARCH.md
@.planning/phases/07-synth-automation-lfo/07-PATTERNS.md

# The just-shipped v1.1 contract this plan tests
@.planning/phases/07-synth-automation-lfo/07-01-SUMMARY.md

<interfaces>
<!-- The v1.1 surface from 07-01 (use directly). -->
apollo.synth.spec: SPEC_VERSION=="1.1", LfoWave(SINE=0,TRIANGLE=1,SQUARE=2), LfoTarget(LEVEL=0,PITCH=1),
  LfoParams(rate,depth,wave,target) frozen, FmParams(..., lfo: LfoParams|None=None), dsp_string(p).
apollo.synth.manifest: load_manifest(path, pair_path) -> FmParams; SUPPORTED_VERSIONS={"1.0","1.1"};
  rejects lfo under "1.0" (message contains "1.1"); validates rate[0.05,20]/depth[0,1]/wave{0,1,2}/target{0,1}.
apollo.synth.render: render(params, notes, *, pair_path) -> np.ndarray (deterministic, normalized).

Existing test helpers to EXTEND (tests/test_synth_render.py):
- _DEFAULT_OP dict (lines 34-41), _write_call_mid (lines 44-59) — REUSE unchanged (3 notes @0.5s onsets, 120bpm).
- _fm_params(**op_overrides) (lines 62-68): pops algorithm/gain from kwargs, builds 3 identical ops. ADD: pop `lfo=` kwarg and thread it into FmParams(..., lfo=lfo).
- _manifest_dict(**op_overrides) (lines 71-82): hardcodes spec_version "1.0" (line 78). ADD: pop `lfo=` kwarg (include the lfo dict in the manifest when given) AND pop `spec_version=` override (default "1.0") so v1.1 fixtures build.
- importorskip("dawdreamer") at line 91; render/render_call_wav/load_notes imported after it (lines 93-94).
- Existing analogs to mirror: test_render_deterministic (97-105) [np.array_equal], test_timbre_discriminable (130-150) [cos<0.999 through MelExtractor], test_mel_contract (117-127) [(96,128) float32], test_no_clipping (108-114) [<=1.0], the always-run validation block test_manifest_bad_version/ratio_out_of_range/nan_field (172-211) [pytest.raises(IngestError, match=...)].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend test helpers (lfo= + spec_version=) and add LFO validation tests (no dawdreamer)</name>
  <read_first>
    - tests/test_synth_render.py (FULL FILE — helpers lines 34-86, validation block lines 164-211)
    - apollo/synth/manifest.py (the v1.1 lfo validation from 07-01 — match the IngestError message tokens)
    - .planning/phases/07-synth-automation-lfo/07-PATTERNS.md (§"tests/test_synth_render.py" — helper-kwarg pattern + manifest-validation mirrors)
    - .planning/phases/07-synth-automation-lfo/07-RESEARCH.md (§"Validation Architecture" Req->Test map rows for accepts_v11 / requires_v11 / rate / wave / nan)
  </read_first>
  <action>
    Edit `tests/test_synth_render.py`. First, extend the two helpers:
    - `_fm_params`: pop a `lfo=` kwarg (default None) before building ops; thread `lfo=lfo` into the returned `FmParams(...)`. Import `LfoParams` at the top (alongside the existing `FmParams, OperatorParams` import from apollo.synth.spec).
    - `_manifest_dict`: pop `spec_version=` (default `"1.0"`) and `lfo=` (default None) from op_overrides BEFORE building the op dict; set the returned dict's `"spec_version"` to that value, and include `"lfo": lfo` only when lfo is not None.

    Then add these tests in the ALWAYS-RUN validation section (below the importorskip, near lines 172-211 — they call `load_manifest` directly, no dawdreamer):
    - `test_manifest_accepts_v11`: a valid v1.1 manifest WITH an lfo block loads and returns FmParams whose `.lfo` is not None (assert rate/depth/wave/target round-trip). KEEP the existing `test_manifest_bad_version` ("9.9" rejected, match="spec_version").
    - `test_manifest_v10_no_lfo_still_loads`: a v1.0 manifest (no lfo) loads with `.lfo is None` (backward-compat at the loader).
    - `test_lfo_requires_v11`: `_manifest_dict(spec_version="1.0", lfo={...})` → `pytest.raises(IngestError, match="1.1")`.
    - `test_lfo_rate_out_of_range`: lfo.rate=99.0 → `pytest.raises(IngestError, match="lfo rate")` (mirror test_manifest_ratio_out_of_range).
    - `test_lfo_wave_bad_enum`: lfo.wave=9 → `pytest.raises(IngestError, match="lfo wave")`.
    - `test_lfo_target_bad_enum`: lfo.target=5 → `pytest.raises(IngestError, match="lfo target")`.
    - `test_lfo_nan`: lfo.rate=float("nan") (or depth) → `pytest.raises(IngestError, match="finite")` (mirror test_manifest_nan_field).
    Use a valid lfo dict `{"rate":6.0,"depth":0.8,"wave":0,"target":0}` as the base and mutate one field per test (mirror the per-test `_manifest_dict()`-then-mutate style).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q tests/test_synth_render.py -k "manifest_accepts_v11 or v10_no_lfo or lfo_requires or lfo_rate or lfo_wave or lfo_target or lfo_nan or manifest_bad_version" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def test_manifest_accepts_v11' tests/test_synth_render.py`.
    - `grep -q 'def test_lfo_requires_v11' tests/test_synth_render.py` and `grep -q 'match="1.1"' tests/test_synth_render.py`.
    - `grep -q 'def test_lfo_rate_out_of_range' tests/test_synth_render.py` and `grep -q 'def test_lfo_wave_bad_enum' tests/test_synth_render.py` and `grep -q 'def test_lfo_nan' tests/test_synth_render.py`.
    - Helpers extended: `_fm_params` threads an `lfo=` kwarg and `_manifest_dict` accepts `spec_version=` + `lfo=` overrides (grep `lfo` and `spec_version` near the helper definitions).
    - The Task-1 verify command shows the new validation tests PASSING and 0 failures (these run without dawdreamer).
    - Existing validation tests still pass: `.venv/bin/python -m pytest -q tests/test_synth_render.py -k manifest` exits 0.
  </acceptance_criteria>
  <done>Helpers carry lfo=/spec_version= kwargs; 6+ new always-run validation tests cover v1.1 acceptance, lfo-requires-1.1, range/enum/NaN rejection — all passing without dawdreamer.</done>
</task>

<task type="auto">
  <name>Task 2: Add LFO render tests — bit-identity, determinism, time-variation, mel/no-clip (dawdreamer)</name>
  <read_first>
    - tests/test_synth_render.py (FULL FILE — render-test analogs lines 97-161: test_render_deterministic, test_timbre_discriminable, test_mel_contract, test_no_clipping; importorskip line 91)
    - apollo/synth/spec.py (dsp_string + the no-lfo verbatim-v1.0 branch from 07-01 — the bit-identity test asserts dsp_string(no_lfo) emits no lfo_ tokens)
    - .planning/phases/07-synth-automation-lfo/07-01-SUMMARY.md (the exact LFO wiring + any deviation)
    - .planning/phases/07-synth-automation-lfo/07-RESEARCH.md (§"Validation Architecture" rows: absent_bit_identical, render_deterministic, time_varies cos<0.999 @6Hz, depth0_matches_static; §"Common Pitfalls" Pitfall 4 — pin 6 Hz, NOT 0.05 Hz; Pitfall 5 square-wave peaks)
  </read_first>
  <action>
    Add these tests in the dawdreamer-guarded section (after the importorskip, alongside test_render_deterministic lines 97-161). All renders are in-process to `tmp_path` via `_write_call_mid` + `load_notes` (mirror existing render tests). Import `dsp_string` and `LfoParams` from apollo.synth.spec at the top.

    - `test_lfo_render_deterministic`: build `_fm_params(lfo=LfoParams(6.0,0.8,0,0))`; render twice; `assert np.array_equal(a, b)`. (Mirror test_render_deterministic. RESEARCH VERIFIED feasible.) SYNTH-01 SC#3.
    - `test_lfo_absent_bit_identical`: render `_fm_params()` (no lfo) twice → `np.array_equal`; AND assert `"lfo_" not in dsp_string(_fm_params())` (no-lfo emits the verbatim v1.0 source — the backward-compat-by-same-source guarantee, RESEARCH Pitfall 1 / Wave-0 gap; no committed `.wav`). SYNTH-01 SC#2.
    - `test_lfo_depth0_matches_static`: `np.array_equal(render(_fm_params(lfo=LfoParams(6.0,0.0,0,0)), notes, pair_path=...), render(_fm_params(), notes, pair_path=...))` — a depth-0 lfo render equals the no-lfo (static) render bit-for-bit (RESEARCH VERIFIED max diff 0.0). If the executor finds depth-0 audio is NOT bit-identical (a multiply-by-1 Faust codegen difference), record it in the SUMMARY and relax to a mel-cosine ≥ 0.9999 near-identity assertion — but try `np.array_equal` first (RESEARCH measured 0.0 diff this session).
    - `test_lfo_time_varies`: render an lfo patch at rate=6 Hz (`LfoParams(6.0,0.8,0,0)`) and a depth-0 patch (`LfoParams(6.0,0.0,0,0)`); write each to a `tmp_path` `.wav` at `SR` via `soundfile`; pass both through `MelExtractor()`; flatten; assert `torch.nn.functional.cosine_similarity(mel_lfo, mel_static, dim=0).item() < 0.999` (mirror test_timbre_discriminable). Pin the rate at 6 Hz — do NOT test the 0.05 Hz edge (Pitfall 4). SYNTH-01 SC#1.
    - `test_lfo_mel_contract`: an lfo render → write `.wav` → `MelExtractor()` → assert shape `(96,128)` and dtype `torch.float32` (mirror test_mel_contract; SYNTH-01 SC#5).
    - `test_lfo_no_clipping`: a deep square-wave lfo render (`_fm_params(lfo=LfoParams(6.0,1.0,2,0))` to stress peaks — Pitfall 5) → `assert np.max(np.abs(audio)) <= 1.0` (mirror test_no_clipping).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import dawdreamer" 2>/dev/null && .venv/bin/python -m pytest -q tests/test_synth_render.py -k "lfo_render_deterministic or lfo_absent or lfo_depth0 or lfo_time or lfo_mel or lfo_no_clip" 2>&1 | tail -6 || echo "SKIP: dawdreamer absent — render tests importorskip"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def test_lfo_render_deterministic' tests/test_synth_render.py` and `grep -q 'def test_lfo_absent_bit_identical' tests/test_synth_render.py` and `grep -q 'def test_lfo_depth0_matches_static' tests/test_synth_render.py`.
    - `grep -q 'def test_lfo_time_varies' tests/test_synth_render.py` and the file contains a `cos` / `cosine_similarity` assertion with `< 0.999` for the lfo-vs-static mel comparison.
    - `grep -q 'def test_lfo_mel_contract' tests/test_synth_render.py` ((96,128) float32) and `grep -q 'def test_lfo_no_clipping' tests/test_synth_render.py`.
    - The bit-identity test asserts `"lfo_" not in dsp_string(...)` for a no-lfo patch (grep `lfo_` and `not in` near test_lfo_absent_bit_identical) — no committed `.wav` fixture (`! ls tests/*.wav 2>/dev/null` and no new `.wav` under data/).
    - The time-variation test rate is 6 Hz, NOT 0.05 Hz (grep the test body for `6.0` / `6,`; ensure no `0.05` rate in the time-variation test).
    - With dawdreamer present, the Task-2 verify command shows these 6 render tests PASSING with 0 failures; on non-arm64 CI it prints the SKIP line.
    - Full suite green: `.venv/bin/python -m pytest -q` exits 0.
  </acceptance_criteria>
  <done>Six dawdreamer-guarded LFO render tests prove bit-identity (no-lfo + depth-0), determinism, 6 Hz mel time-variation (cos<0.999), the (96,128) contract, and no-clip — all passing; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author/disk -> manifest.py | Test fixtures exercise the untrusted-manifest boundary: malformed/out-of-range/wrong-version lfo blocks must be rejected fail-loud |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering (verification) | tests/test_synth_render.py | mitigate | Validation tests (`test_lfo_requires_v11`, `test_lfo_rate_out_of_range`, `test_lfo_wave_bad_enum`, `test_lfo_target_bad_enum`, `test_lfo_nan`) assert the manifest boundary rejects every malformed lfo block — the T-07-02/T-07-03 mitigations from 07-01 are proven, not assumed. |
| T-07-07 | Repudiation / regression | tests/test_synth_render.py | mitigate | Bit-identity (`test_lfo_absent_bit_identical`, `test_lfo_depth0_matches_static`) + determinism (`test_lfo_render_deterministic`) tests pin the reproducible-training-data guarantee so a future dawdreamer/Faust bump that breaks determinism is caught. |
| T-07-08 | Information Disclosure | test fixtures | accept | All fixtures are in-process numeric manifests in tmp_path; no committed audio, no PII, no network. Rationale: trusted-local test workflow. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q tests/test_synth_render.py` exits 0 (all new LFO + existing Phase-6 tests pass when dawdreamer present; render tests skip otherwise).
- `.venv/bin/python -m pytest -q` exits 0 (full suite green — no regression to the 177 prior tests).
- The 9 RESEARCH Validation-Architecture rows are all represented: bit-identity, determinism, time-variation, depth0==static, accepts-v11, requires-v11, rate/enum/nan validation, mel-contract, no-clip.
</verification>

<success_criteria>
- Helpers accept lfo=/spec_version= kwargs; no committed `.wav` fixtures.
- Always-run validation tests cover v1.1 acceptance + lfo-requires-1.1 + range/enum/NaN rejection (run without dawdreamer).
- dawdreamer-guarded render tests prove SYNTH-01 SC#1 (time-variation), SC#2 (no-lfo + depth-0 bit-identity), SC#3 (determinism), SC#5 (mel contract) + no-clip.
- Full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-synth-automation-lfo/07-02-SUMMARY.md`. Record: total test count before/after, whether `test_lfo_depth0_matches_static` held bit-identical (`np.array_equal`) or required the mel-cosine relaxation, the measured cos value for `test_lfo_time_varies` at 6 Hz (compare to RESEARCH's 0.9990), and any dawdreamer skip behavior observed.
</output>
