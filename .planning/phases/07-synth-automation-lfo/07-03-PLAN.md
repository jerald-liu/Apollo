---
phase: 07-synth-automation-lfo
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - data/pairs/CORPUS-CONVENTIONS.md
autonomous: true
requirements: [SYNTH-01]
must_haves:
  truths:
    - "CORPUS-CONVENTIONS.md documents the optional lfo block as a hand-authorable part of call_fm.json (SC#6)"
    - "The schema section states spec_version may be '1.0' OR '1.1', and that an lfo block requires '1.1'"
    - "The lfo field table documents rate [0.05,20.0], depth [0,1], wave {0,1,2}=sine/triangle/square, target {0,1}=level/pitch"
    - "The tremolo and vibrato math (the unipolar formula and pow(2,cents/1200) with the 50-cent default) is documented for Phase 5 browser-synth parity (SC#4)"
  artifacts:
    - path: "data/pairs/CORPUS-CONVENTIONS.md"
      provides: "v1.1 lfo schema doc: optional lfo JSON block, field table rows, Phase-5 parity formulas, low-rate partial-sweep note"
      contains: "lfo"
      min_lines: 110
  key_links:
    - from: "data/pairs/CORPUS-CONVENTIONS.md"
      to: "apollo/synth/spec.py"
      via: "documents the same lfo schema (rate/depth/wave/target + SPEC_VERSION 1.1) the spec defines"
      pattern: "spec_version|lfo"
---

<objective>
Document the optional `lfo` block so it is hand-authorable (SYNTH-01 SC#6) and so Phase 5's browser synth can mirror the exact LFO math (SC#4). Extend the existing `call_fm.json` schema section of `CORPUS-CONVENTIONS.md`: note the `{"1.0","1.1"}` version acceptance + the lfo-requires-1.1 rule, add the optional `lfo` JSON example, add the four lfo field-table rows, document the tremolo/vibrato formulas for Phase-5 parity, and note the short-gesture partial-sweep behavior.

This plan touches ONLY the doc — disjoint from 07-02 (tests), so the two run in parallel in Wave 2.

Purpose: SC#6 (hand-authorable) and SC#4 (single source of truth, documented for Phase 5) are doc-completion criteria — the schema must be authorable without reading source.
Output: extended `data/pairs/CORPUS-CONVENTIONS.md`.

**Orchestrator working defaults to document (mirror 07-01; NOT user-locked):** vibrato max-cents = 50 (note discuss-phase could refine to 100); LFO applies to all carriers uniformly; reserved-but-unimplemented slots `wave 3=saw`, `target 2=FM-mod-depth` (mention as reserved).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/07-synth-automation-lfo/07-RESEARCH.md
@.planning/phases/07-synth-automation-lfo/07-PATTERNS.md

# The shipped v1.1 schema + exact wiring this doc must match
@.planning/phases/07-synth-automation-lfo/07-01-SUMMARY.md

<interfaces>
<!-- Doc must match the 07-01 schema exactly. -->
Existing CORPUS-CONVENTIONS.md structure to EXTEND (data/pairs/CORPUS-CONVENTIONS.md):
- "The call_fm.json schema (spec_version \"1.0\")" heading (line 37).
- JSON example block (lines 41-52).
- field table `| Field | Type | Range | Notes |` (lines 54-65), with the `spec_version` row at line 56 ("must equal \"1.0\"").

v1.1 lfo schema (from 07-01, RESEARCH §"Manifest Schema"):
  lfo: { "rate": 6.0, "depth": 0.8, "wave": 0, "target": 0 }   // optional; omit ⇒ bit-identical to Phase 6
  rate   number [0.05, 20.0] Hz
  depth  number [0.0, 1.0]
  wave   int    {0=sine, 1=triangle, 2=square}   (3=saw reserved, not implemented)
  target int    {0=level/tremolo, 1=pitch/vibrato}   (2=FM-mod-depth reserved, not implemented)

Phase-5 parity formulas (RESEARCH Open Q 3):
  bipolar LFO lfo_bi ∈ [-1,1]; unipolar lfo_uni = (lfo_bi + 1)/2
  tremolo (level): lvl_mod = 1 - depth*(1 - lfo_uni)   (multiply each carrier by lvl_mod)
  vibrato (pitch): pitch_mul = pow(2, (lfo_bi * depth * 50) / 1200)   (50 cents default; multiply carrier freq by pitch_mul)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the optional lfo block + v1.1 in CORPUS-CONVENTIONS.md</name>
  <read_first>
    - data/pairs/CORPUS-CONVENTIONS.md (FULL FILE — schema heading line 37, JSON example 41-52, field table 54-65, spec_version row line 56)
    - .planning/phases/07-synth-automation-lfo/07-01-SUMMARY.md (the exact emitted wiring + the final max-cents literal + carrier-application choice — document what shipped)
    - .planning/phases/07-synth-automation-lfo/07-PATTERNS.md (§"data/pairs/CORPUS-CONVENTIONS.md" — exact edit list)
    - .planning/phases/07-synth-automation-lfo/07-RESEARCH.md (§"Manifest Schema" field table, §"Open Questions" 3 — Phase-5 parity formulas, §"Common Pitfalls" Pitfall 4 — partial-sweep note)
  </read_first>
  <action>
    Edit `data/pairs/CORPUS-CONVENTIONS.md` (mirror the existing JSON-example + field-table doc style; do NOT restructure the file):

    1. Update the schema heading (line 37) from `(spec_version "1.0")` to `(spec_version "1.0" or "1.1")`.

    2. Update the `spec_version` field-table row (line 56): instead of "must equal \"1.0\"", state it must be `"1.0"` OR `"1.1"`; an unknown version → `IngestError`; an `lfo` block REQUIRES `"1.1"` (a `"1.0"` manifest carrying `lfo` is rejected). Note v1.0 manifests still render bit-identically (no behavior change for existing pairs).

    3. Add an OPTIONAL `lfo` block to the JSON example (after `gain`, with a comment that it is optional and omittable for a static Phase-6-identical patch). Use a JSON-with-comment style consistent with the doc, e.g.:
       ```jsonc
       "gain": 0.5,
       "lfo": { "rate": 6.0, "depth": 0.8, "wave": 0, "target": 0 }   // optional; omit ⇒ static (Phase-6-identical)
       ```
       (If the doc's JSON block must stay strict JSON, add the lfo block in a SEPARATE fenced `jsonc` example below the main one with a sentence introducing it as the optional v1.1 extension.)

    4. Add four rows to the field table (mirroring the existing row format):
       - `lfo.rate` | number | `0.05 .. 20.0` (Hz) | LFO speed; low rates give a slow partial sweep over the 0.5–1.5 s gesture.
       - `lfo.depth` | number | `0.0 .. 1.0` | Modulation amount; `0` ⇒ no audible modulation.
       - `lfo.wave` | int | `{0,1,2}` | `0=sine, 1=triangle, 2=square`. (`3=saw` reserved for a later spec, not implemented.)
       - `lfo.target` | int | `{0,1}` | `0=level (tremolo)`, `1=pitch (vibrato)`. (`2=FM-mod-depth` reserved, not implemented.)

    5. Add a short "LFO (v1.1) — synth modulation" subsection documenting:
       - That the LFO is ONE global LFO per patch applied to ALL carriers uniformly (orchestrator default).
       - The Phase-5 parity math (so the browser synth matches the renderer EXACTLY): bipolar `lfo_bi ∈ [-1,1]`, unipolar `lfo_uni = (lfo_bi + 1)/2`; tremolo `lvl_mod = 1 - depth*(1 - lfo_uni)`; vibrato `pitch_mul = pow(2, (lfo_bi * depth * 50) / 1200)` (state the 50-cent default explicitly, matching the literal that shipped in 07-01 — read 07-01-SUMMARY to confirm; note discuss-phase could refine to 100).
       - The per-note phase note: the LFO phase resets to 0 at each note onset (deterministic, onset-relative — `os.osc` resets per poly voice).
       - The Pitfall-4 partial-sweep note: at low rates over a 0.5–1.5 s gesture the LFO contributes only a partial cycle — this is the intended slow-drift expressive behavior.

    Keep the rest of the doc (corpus size, file layout, workflow steps, known-limitation, validation) unchanged.
  </action>
  <verify>
    <automated>grep -q 'spec_version "1.0" or "1.1"' data/pairs/CORPUS-CONVENTIONS.md && grep -q 'lfo.rate' data/pairs/CORPUS-CONVENTIONS.md && grep -q 'pow(2' data/pairs/CORPUS-CONVENTIONS.md && grep -qi 'tremolo' data/pairs/CORPUS-CONVENTIONS.md && grep -qi 'vibrato' data/pairs/CORPUS-CONVENTIONS.md && echo "doc OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'spec_version "1.0" or "1.1"' data/pairs/CORPUS-CONVENTIONS.md` (heading updated).
    - The spec_version field-table row mentions both versions AND that lfo requires "1.1": `grep -q '1.1' data/pairs/CORPUS-CONVENTIONS.md`.
    - All four lfo rows present: `grep -q 'lfo.rate' data/pairs/CORPUS-CONVENTIONS.md` and `grep -q 'lfo.depth' data/pairs/CORPUS-CONVENTIONS.md` and `grep -q 'lfo.wave' data/pairs/CORPUS-CONVENTIONS.md` and `grep -q 'lfo.target' data/pairs/CORPUS-CONVENTIONS.md`.
    - Waveform + target enums documented: `grep -qi 'sine' data/pairs/CORPUS-CONVENTIONS.md` and `grep -qi 'square' data/pairs/CORPUS-CONVENTIONS.md` and `grep -qi 'tremolo' data/pairs/CORPUS-CONVENTIONS.md` and `grep -qi 'vibrato' data/pairs/CORPUS-CONVENTIONS.md`.
    - Phase-5 parity math present: `grep -q 'pow(2' data/pairs/CORPUS-CONVENTIONS.md` (vibrato) and the unipolar/tremolo formula appears (`grep -q 'lvl_mod' data/pairs/CORPUS-CONVENTIONS.md` or an explicit `1 - depth` formula).
    - The 50-cent default is stated: `grep -q '50' data/pairs/CORPUS-CONVENTIONS.md` near the vibrato formula.
    - The Task-1 verify command prints `doc OK`.
  </acceptance_criteria>
  <done>CORPUS-CONVENTIONS.md documents the optional lfo block (JSON example + 4 field rows), the {1.0,1.1} version rule with lfo-requires-1.1, the tremolo/vibrato parity math with the 50-cent default, the per-note phase reset, and the low-rate partial-sweep behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits documentation only — no code, no input parsing, no runtime surface. The enforced boundaries live in 07-01 (manifest.py / spec.py). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Tampering (doc drift) | data/pairs/CORPUS-CONVENTIONS.md | mitigate | The documented lfo ranges/enums/version rule MUST match the enforced values in manifest.py/spec.py (07-01). Acceptance greps pin the field ranges and the {1.0,1.1}+lfo-requires-1.1 rule so the doc cannot silently diverge from the validator. |
| T-07-10 | Information Disclosure | documentation | accept | Public authoring doc; contains only schema/formula info, no secrets. Rationale: intended-public corpus-authoring guide. |
</threat_model>

<verification>
- `grep` checks above all pass (heading, four lfo rows, enums, parity math, 50-cent default, version rule).
- The documented ranges match manifest.py: rate [0.05,20.0], depth [0,1], wave {0,1,2}, target {0,1} (cross-check against apollo/synth/manifest.py LFO_* constants and the spec.py enums from 07-01).
- No code/test files changed by this plan (`git diff --name-only` shows only data/pairs/CORPUS-CONVENTIONS.md).
</verification>

<success_criteria>
- The optional lfo block is hand-authorable from the doc alone (SC#6): JSON example + field table + version rule.
- The LFO math is documented for Phase-5 parity (SC#4): tremolo + vibrato formulas with the 50-cent default and the per-note phase reset.
- Documented ranges/enums match the 07-01 validator exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/07-synth-automation-lfo/07-03-SUMMARY.md`. Record: the final vibrato max-cents value documented (confirm it matches the literal that shipped in 07-01), whether the JSON example used inline `jsonc` comments or a separate fenced block, and confirmation that the documented ranges match the manifest.py LFO_* constants.
</output>
