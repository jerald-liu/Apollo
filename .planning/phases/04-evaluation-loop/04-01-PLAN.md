---
phase: 04-evaluation-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - apollo/eval/__init__.py
  - apollo/eval/web/__init__.py
  - eval/rubric.md
  - eval/render_manifests/.gitkeep
autonomous: true
requirements: [EVAL-01]
requirements_addressed: [EVAL-01]

assumptions:
  - "CLI dispatcher: option (A) per RESEARCH §CLI Subcommand Pattern — three standalone scripts under apollo/scripts/, `apollo eval <X>` is shorthand for `python -m apollo.scripts.eval_<X>`. README documents the alias. No `[project.scripts]` dispatcher added."
  - "Eval extras: flask>=3.0, pandas>=2.0, matplotlib>=3.7, jupyter>=1.0 per RESEARCH §Standard Stack."

must_haves:
  truths:
    - "`pip install -e .[eval]` installs flask, pandas, matplotlib, jupyter."
    - "`eval/rubric.md` exists at repo root with anchor text for both dimensions per D-01..D-03."
    - "`apollo.eval` and `apollo.eval.web` are importable packages."
    - "`eval/scores.jsonl`, `eval/runs.jsonl`, `eval/render_manifests/`, and `data/pairs/*/eval/` are gitignored."
  artifacts:
    - path: "pyproject.toml"
      provides: "[project.optional-dependencies] eval extra"
      contains: 'eval = ["flask>=3.0"'
    - path: ".gitignore"
      provides: "eval data file ignore patterns"
      contains: "eval/scores.jsonl"
    - path: "apollo/eval/__init__.py"
      provides: "package surface (placeholder; final exports filled by 04-02)"
    - path: "apollo/eval/web/__init__.py"
      provides: "Flask sub-package marker"
    - path: "eval/rubric.md"
      provides: "Rubric anchors for fit + coherence (D-01..D-03)"
      contains: "Call-Response Fit"
  key_links:
    - from: "pyproject.toml"
      to: "Phase-4 Python tooling"
      via: "optional-dependencies extra"
      pattern: 'eval = \['
    - from: "eval/rubric.md"
      to: "grader UI (Phase 4 plan 04)"
      via: "anchor strip text per D-02"
      pattern: "1 unrelated"
---

<objective>
Scaffold the Phase-4 foundations: declare the `eval` Python extra, gitignore the regenerable JSONL/manifest artifacts, create empty `apollo/eval/` and `apollo/eval/web/` packages, and commit the canonical `eval/rubric.md` with anchor wording (EVAL-01).

Purpose: Every subsequent plan in Phase 4 either imports from `apollo.eval` or reads `eval/rubric.md`. This plan unblocks them with one parallel-safe Wave 1 task set.
Output: pyproject.toml updated, .gitignore updated, two new Python packages (empty `__init__.py`), `eval/rubric.md` (committed source of truth), `eval/render_manifests/.gitkeep`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-evaluation-loop/04-CONTEXT.md
@.planning/phases/04-evaluation-loop/04-RESEARCH.md
@.planning/phases/04-evaluation-loop/04-PATTERNS.md
@pyproject.toml
@.gitignore
@apollo/ingest/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: pyproject `eval` extra + .gitignore additions</name>
  <files>pyproject.toml, .gitignore</files>
  <read_first>
    - pyproject.toml (read fully — find `[project.optional-dependencies]` section)
    - .gitignore (read fully)
    - .planning/phases/04-evaluation-loop/04-RESEARCH.md §Standard Stack (lines 109–125)
  </read_first>
  <action>
    In `pyproject.toml`, locate `[project.optional-dependencies]`. The existing entry is `dev = ["pytest>=7.0"]`. Add on the next line (inside the same table):
    ```
    eval = ["flask>=3.0", "pandas>=2.0", "matplotlib>=3.7", "jupyter>=1.0"]
    ```
    Do not touch the `dev` line. Do not change `requires-python`, `dependencies`, or any other field.

    In `.gitignore`, append a new section at the bottom:
    ```
    # Phase 4 — eval loop regenerable data
    eval/scores.jsonl
    eval/runs.jsonl
    eval/render_manifests/*.json
    !eval/render_manifests/.gitkeep
    data/pairs/*/eval/
    ```
    (The `!eval/render_manifests/.gitkeep` negation keeps the placeholder file tracked; Task 3 creates it.)

    Do NOT gitignore `eval/rubric.md` or `eval/delta.ipynb` — those are committed sources of truth (per RESEARCH §Recommended Project Structure).
  </action>
  <verify>
    <automated>grep -q 'eval = \["flask>=3.0"' pyproject.toml && grep -q '^eval/scores.jsonl$' .gitignore && grep -q '^!eval/render_manifests/.gitkeep$' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^eval = ' pyproject.toml` returns 1
    - `grep -c '^eval/scores.jsonl$' .gitignore` returns 1
    - `grep -c '^eval/runs.jsonl$' .gitignore` returns 1
    - `grep -c '^data/pairs/\*/eval/$' .gitignore` returns 1
    - `python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert 'flask>=3.0' in d['project']['optional-dependencies']['eval']"` exits 0
  </acceptance_criteria>
  <done>
    pyproject.toml declares the `eval` extra with flask/pandas/matplotlib/jupyter; .gitignore covers scores.jsonl, runs.jsonl, manifest JSONs (with .gitkeep allow-list), and per-pair eval subdirs.
  </done>
</task>

<task type="auto">
  <name>Task 2: apollo/eval and apollo/eval/web package skeletons</name>
  <files>apollo/eval/__init__.py, apollo/eval/web/__init__.py</files>
  <read_first>
    - apollo/ingest/__init__.py (full file — lines 1–39 are the analog pattern)
    - .planning/phases/04-evaluation-loop/04-PATTERNS.md §"apollo/eval/__init__.py (package re-export)"
  </read_first>
  <action>
    Create `apollo/eval/__init__.py` with this exact content:
    ```python
    """Apollo eval package — held-out enumeration, run identity, score store, ship gate.

    Public surface (filled in as Phase 4 plans land; placeholder during Wave 1):
        - compute_run_id        (apollo/eval/run_id.py)
        - enumerate_heldout     (apollo/eval/heldout.py)
        - append_run            (apollo/eval/runs_log.py)
        - append_score, append_score_pair, load_scores  (apollo/eval/scores_log.py)
        - check_ship_gate       (apollo/eval/ship_check.py)
        - write_render_manifest (apollo/eval/render_manifest.py)

    Scaffolded by 04-01-PLAN. Re-exports are added by 04-02-PLAN once modules exist.

    See .planning/phases/04-evaluation-loop/04-CONTEXT.md (D-01..D-17) and
    .planning/phases/04-evaluation-loop/04-RESEARCH.md.
    """
    from __future__ import annotations

    __all__: list[str] = []
    ```

    Create `apollo/eval/web/__init__.py` with this exact content:
    ```python
    """Apollo eval web sub-package — Flask grading UI (D-04, EVAL-02).

    The `create_app` factory is added in 04-04-PLAN. This module exists so
    `apollo.eval.web` is an importable package from Wave 1 onward.

    See .planning/phases/04-evaluation-loop/04-UI-SPEC.md for the UI contract.
    """
    from __future__ import annotations
    ```

    Both files must start with `from __future__ import annotations` per PATTERNS S1.
  </action>
  <verify>
    <automated>python -c "import apollo.eval; import apollo.eval.web; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import apollo.eval; print(apollo.eval.__all__)"` prints `[]`
    - `python -c "import apollo.eval.web"` exits 0
    - `grep -q "from __future__ import annotations" apollo/eval/__init__.py`
    - `grep -q "from __future__ import annotations" apollo/eval/web/__init__.py`
    - `grep -q "D-01..D-17" apollo/eval/__init__.py`
  </acceptance_criteria>
  <done>
    Both packages import cleanly; docstrings cite the relevant CONTEXT/UI-SPEC references; `__all__` is the empty list placeholder.
  </done>
</task>

<task type="auto">
  <name>Task 3: eval/rubric.md + eval/render_manifests/.gitkeep</name>
  <files>eval/rubric.md, eval/render_manifests/.gitkeep</files>
  <read_first>
    - .planning/phases/04-evaluation-loop/04-RESEARCH.md §"Rubric Schema" (lines 626–660)
    - .planning/phases/04-evaluation-loop/04-CONTEXT.md §Rubric (D-01, D-02, D-03)
    - .planning/phases/04-evaluation-loop/04-UI-SPEC.md §Copywriting Contract (anchor strip rows)
  </read_first>
  <action>
    Create `eval/rubric.md` with the following content verbatim (delivers EVAL-01, written-anchors-per-scale-point per D-02; UI anchor strips in UI-SPEC reference this file as source of truth):

    ```markdown
    # Apollo Grading Rubric

    The rubric is the source of truth for what "improvement" means in the active-learning
    loop (EVAL-05). The grading UI shows shortened anchor strips inline; full anchor text
    lives here. Do not edit the dimensions or anchor wording without re-scoring at least
    one historical run — anchor drift invalidates cross-iteration deltas (D-02).

    ## How to grade

    1. After a training run, render held-out responses:
       `python -m apollo.scripts.eval_render <checkpoint_path> [--iteration]`
       Then open Ableton and run the `m4l/ApolloRender.amxd` device against
       `eval/render_manifests/active.json` to bounce per-pair `response.wav`.
    2. Start the grading UI:
       `python -m apollo.scripts.eval_grade data/pairs/ --run-id <run_id>`
    3. For each held-out pair: hit Space, listen call → response, press `1`–`5` for fit,
       `Shift+1`–`Shift+5` for coherence, optionally `n` to note, `Enter` to submit.
    4. Sessions are resumable — close the tab and re-launch; already-scored pairs show ✓.

    ## Dimensions

    ### Call-Response Fit (1–5) — THE SHIP-GATING DIMENSION (D-14)

    - **1 — unrelated.** The response could have followed any call. No shared key, register,
      rhythm, or gestural logic.
    - **2 — marginally related.** Some shared element (key, register, or rhythm) but no real
      call-and-response logic — the response is in the same neighbourhood, not in conversation.
    - **3 — plausible.** Reasonable musical reply; nothing distinctive. A competent generic
      response, not specifically *yours*.
    - **4 — strong.** Clearly answers the call; a choice you might make. The gesture's logic
      tracks back to something in the call.
    - **5 — exactly what I'd play.** Indistinguishable from your own authoring intent. The
      response feels inevitable given the call.

    ### Musical Coherence (1–5) — TRACKED, NOT GATING (D-14)

    - **1 — wrong notes.** Out-of-scale without purpose, untuned, or structurally broken.
    - **2 — coherent but awkward.** Notes work but phrasing is off — timing, contour, or
      shape feels wrong as a standalone phrase.
    - **3 — coherent statement.** Holds together as a phrase standalone, separable from the call.
    - **4 — good phrase shape.** Intentional timing and contour; could appear in a sketch.
    - **5 — could ship as-is.** Could appear in a finished piece without further editing.

    ## Free-text note (optional, D-03)

    The note field captures qualitative signal for the next authoring round. Useful patterns:
    - "tonic-bound" — model keeps landing on the root
    - "rhythm matches but pitch random" — timing transfer works, pitch logic doesn't
    - "second note feels late" — micro-timing miss
    - "this is the gesture I always over-author" — corpus bias surfaced

    Notes are scanned by hand when reviewing `eval/scores.jsonl` and when deciding what to
    author next. They are not parsed or aggregated.

    ## Iteration marker (D-16, EVAL-05)

    A run only counts toward the ship gate if it was rendered with `--iteration`. Sweeps,
    debug runs, and ablations stay unmarked. The ship gate (`apollo eval ship-check`)
    looks at the last two iteration-marked runs and checks that each beat its predecessor
    in `runs.jsonl` on mean fit (D-14, D-16).

    Mark only runs that represent a true corpus expansion / retraining round you want
    measured. Over-marking dilutes the gate signal (RESEARCH Pitfall 8).

    ## Blind grading (D-05)

    The UI hides run id / checkpoint / timestamp during scoring. A "🔎 reveal" link in the
    pair footer surfaces them post-hoc. The blind default is a behavioural nudge, not a
    security barrier — the user is both training target and grader; honour mode applies.
    ```

    Then create the empty marker file `eval/render_manifests/.gitkeep` (zero bytes is fine; an empty file is sufficient — git tracks empty files when explicitly added).
  </action>
  <verify>
    <automated>test -f eval/rubric.md && test -f eval/render_manifests/.gitkeep && grep -q "Call-Response Fit (1–5)" eval/rubric.md && grep -q "Musical Coherence (1–5)" eval/rubric.md && grep -q "exactly what I'd play" eval/rubric.md</automated>
  </verify>
  <acceptance_criteria>
    - `eval/rubric.md` exists and contains both dimension headings ("Call-Response Fit", "Musical Coherence") and all five anchors per dimension (grep for "1 — ", "2 — ", "3 — ", "4 — ", "5 — " each occurs at least twice).
    - File mentions D-14 (ship-gating reference), D-16 (iteration marker), D-02 (anchor stability), D-05 (blind grading).
    - `eval/render_manifests/.gitkeep` exists (touch-file).
    - `git check-ignore eval/rubric.md` exits non-zero (not ignored).
    - `git check-ignore eval/render_manifests/.gitkeep` exits non-zero (allow-listed by negation in .gitignore from Task 1).
  </acceptance_criteria>
  <done>
    `eval/rubric.md` committed with the canonical anchor text for both dimensions; `eval/render_manifests/.gitkeep` placeholder lets git track the directory while ignoring transient manifest JSONs.
  </done>
</task>

</tasks>

<verification>
- `python -c "import apollo.eval; import apollo.eval.web"` returns 0
- `grep 'eval = \["flask>=3.0"' pyproject.toml` returns the eval extras line
- `test -f eval/rubric.md && test -f eval/render_manifests/.gitkeep`
- `pip install -e ".[eval]"` succeeds (deferred to first executor that needs Flask; pyproject syntax is validated by the tomllib import in Task 1's acceptance criteria)
</verification>

<success_criteria>
- Phase-4 foundations exist: eval extra declared, .gitignore covers regenerable JSONL/manifest data, `apollo/eval/` + `apollo/eval/web/` are importable, `eval/rubric.md` is the source of truth for D-02 anchor text.
- No code from later plans is preempted — `__init__.py` files are placeholders with empty `__all__`.
</success_criteria>

<output>
Create `.planning/phases/04-evaluation-loop/04-01-SUMMARY.md` after completion.
</output>
